# COUNCIL_LOG.dryrun.md — dry-run (diagnose-only) deliberation record

Rehearsals on historical/scrubbed savestates, run by the human-operated council
grading harness. Entries use the COUNCIL_PROTOCOL.md §12 format with event ids
prefixed `DRYRUN-`. Nothing here is implemented: fixes are written proposals only.
This file is separate from the real COUNCIL_LOG.md and carries no run-state weight.

---

## EVENT DRYRUN-001 — 2026-07-31T23:10:57Z
Trigger: §4.1 watchdog_stall — runner progress watchdog (dexbot/runner.py `_PROGRESS_BUDGET_FRAMES`=30_000, overworld) dumped a savestate after 30k frames (~8 game-min) with the progress sample (map/coords/money/balls/game_state/party-HP) unchanged. Objective 'stall_a' (dry-run: historical, scrubbed name). Evidence: stall_a.ss1, stall_a_state.json, logs/skills.jsonl (skills.jsonl entries are stale test-nav telemetry ~42 min before the dump — not a live trace; progress.jsonl and STATUS.md do not exist yet, pre-run).

Seats: A=claude-sonnet-5  B=claude-opus-4-8[1m] (see Model check)  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet`; self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). **VIOLATION (harness limitation, disclosed per invocation):** claude-opus-5 does not exist in this harness — the only Opus available is Opus 4.8. Spawned via alias `opus`; self-reported `claude-opus-4-8[1m]`. No spawn parameter can produce the section-3 model, so per the invocation's fallback the mismatch is recorded here prominently rather than redone. Seat B ran on Opus 4.8, one minor version below the nominal seat model; provider (Anthropic/Claude) is correct.
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP; account rejected the explicit `gpt-5.2-codex` override ("not supported when using Codex with a ChatGPT account"), so the account-default Codex GPT-5-class model was used. Self-reported "Codex GPT-5-class". Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event: threadId 019fba6f-0737-7760-9398-79a9d37df783.

Exchanges: 3/6

Exchange 1 — Seat A (Diagnostician), position: CODE DEFECT (navigation), not KB error, not pure transient, plus one unresolved layer. (1) `enter_center()` (dexbot/navigation.py:699) walks the bot OUT of a center it is already inside and back in; `_pick_reachable_center()` (dexbot/catching.py:457-465) already has an "already-inside" name-prefix short-circuit that `enter_center` lacks. Stall tile (7,4) facing Down is step 1 of that redundant walk-out down the single-tile south exit corridor. (2) The 30k-frame freeze does NOT reproduce from the savestate (a .ss1 restores game memory, not the Python nav-controller/generator state), so the exact wedge mechanism is inferred, not observed. Severity: High if unfixed (~8 game-min + dump/recovery per hit; watchdog+deferral prevent run-death). Blast radius: WIDE — enter_center runs on every Pokémon Center visit for team assembly, rest of run. Confidence: high on defect 1, medium on defect-2 mechanism. Sources: memory-state read of stall_a.ss1; headless tile/collision map read of MapFRLG (8,0); pokebot-gen3 modules/map_data.py (MapFRLG.LAVENDER_TOWN_POKEMON_CENTER_1F=(8,0)); dexbot/navigation.py, dexbot/catching.py. FINDINGS index empty → no rediscovery.

Exchange 2 — Seat B (Fix Designer), position: Root cause = `enter_center` unconditionally `navigate_to`'s the OUTDOOR door coord even when already inside the interior, routing the bot back out the corridor then in; the stall tile is step 1. Proposed FIX (written only): insert a guard at the top of `enter_center`, before `navigate_to`, mirroring `_pick_reachable_center`'s idiom —
    from modules.map_data import MapFRLG
    from modules.player import get_player_avatar
    if isinstance(center.value[0], MapFRLG):
        try: here_name = MapFRLG(tuple(get_player_avatar().map_group_and_number)).name
        except ValueError: here_name = ""
        if here_name.startswith(center.value[0].name + "_POKEMON_CENTER"):
            return  # already inside target center
The fix does NOT directly repair the inferred freeze wedge; it removes the only code path that sends the bot into the corridor during ENTRY, so the trigger is never met on entry. Alternatives considered and rejected in writing: (a) harden the walker with corridor repath/timeout — treats a symptom of an unreproducible wedge, larger blast radius, doesn't stop the waste; keep as later defense-in-depth; (b) instrumentation only — ships no repair; (c) fix at each caller — duplicates the guard, future callers re-inherit the bug, invariant belongs in enter_center; (d) widen/relocate the exit corridor — ROM map geometry, out of scope. Regression surface: touches enter_center only; baseline 74 passed / 2 failed (BASELINE.md) must be unchanged. Residual risk: the bot must still exit ONCE to reach the catch map — that single corridor pass re-exposes the un-fixed freeze wedge; accepted (frequency drops from out+in+re-entries to one necessary pass; freeze never reproduced deterministically). Sources: dexbot/navigation.py:699-723, dexbot/catching.py:457-465, dexbot/team.py:387-399, dexbot/boxes.py:52-77, BASELINE.md; driver REPRO A/B (below).

Exchange 3 — Seat C (Quality Gate, Codex), verdict: **PASS-WITH-CONCERNS** on the written proposal. Independently verified against the repo that `enter_center()` always calls `navigate_to(center.value[0], center.value[1])` before any inside-check, and that `_pick_reachable_center()` already carries the same inside-center name-prefix short-circuit; the guard matches the diagnosis and removes the redundant maneuver. Judged it acceptable to ship as risk-reduction without root-causing the unreproduced freeze (maneuver is unnecessary, frequent, and directly creates the observed corridor exposure; residual risk correctly stated). Concerns: (i) cite the "Silph Co exit lands on Saffron's door" edge-case as a proper source or drop it — it traces to enter_center's own docstring (dexbot/navigation.py:701-703), a code-comment observation, acceptable but should be labeled as such, not stated as a bare game fact; (ii) caller review is incomplete — local search shows enter_center callers beyond assemble_party/deposit_party_fodder. Rejected-alternatives set judged adequate.

Driver verification of Seat C concern (ii): grep confirms FOUR enter_center callers — dexbot/team.py:396 (assemble_party), dexbot/boxes.py:55 (deposit_party_fodder), dexbot/evolution.py:114 (boxed-species fetch → navigate to PC), dexbot/story.py:2044 (sevii_accept → talk_to_npc(7) at Cinnabar center). All four follow the pattern "enter_center THEN act inside the interior." The guard's postcondition (avatar controllable inside the target interior) holds for all four whether it returns early or performs full entry, so the fix is safe for every caller and removes the same waste from the evolution and Sevii paths. Concern (ii) is valid and, on review, STRENGTHENS the proposal (wider benefit) rather than invalidating it; the proposal's blast-radius note should be updated to name all four callers if implemented.

Verdict: PASS-WITH-CONCERNS (on the written proposal, per dry-run rules — §9 replay/regression gate inapplicable; nothing implemented).
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction. No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): Seat C's (i) docstring-citation labeling and (ii) full-caller review — (ii) resolved by driver verification above; (i) is a documentation nit for implementation time.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the fix remains a written proposal. No commit, no PARKED routing).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, a finding would record: enter_center missing already-inside short-circuit → redundant center exit/re-enter through narrow warp corridors → watchdog stall, fixed by the mirror guard, sources as above).

Driver disclosures (side effects, all restored):
- Ran three headless read-only reproductions on stall_a.ss1 using the persistent-profile emulator (dexbot.emulator.setup_headless_emulator). All skill/telemetry writes were redirected to a scratch dir (monkeypatched runner._checkpoint_phase → no-op, runner._events_path and runner._dump_stall → scratch); logs/skills.jsonl and fixtures/_phases/*.ss1 verified byte-identical (md5) to pre-run backups afterward.
- setup_headless_emulator created a `livingdex` profile dir (pokebot-gen3/profiles/livingdex/{metadata.yml,stats.db}; none existed before). Removed after the runs; no current_state.ss1 was written. Profile tree restored to as-found. (Cf. memory `repro-writes-profile-dir`.)
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, data/, or any dexbot/ source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume).

---

## EVENT DRYRUN-002 — 2026-07-31T23:48:07Z
Trigger: §4.1 watchdog_stall — runner progress watchdog (dexbot/runner.py `_PROGRESS_BUDGET_FRAMES`=30_000, overworld) dumped a savestate after ~30k frames (~8 game-min) with the progress sample (map/coords/money/balls/game_state/party-HP) unchanged. Objective 'stall_b' (dry-run: historical, scrubbed name). Evidence: stall_b.ss1, stall_b_state.json, logs/skills.jsonl+progress.jsonl+STATUS.md (all absent/empty — pre-run harness, so which skill produced 'stall_b' is not telemetry-confirmed).

Seats: A=claude-sonnet-5  B=claude-opus-4-8[1m] (see Model check)  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet`; self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). **VIOLATION (harness limitation, disclosed per invocation):** claude-opus-5 does not exist in this harness — the only Opus available is Opus 4.8. Spawned via alias `opus`; self-reported `claude-opus-4-8`. No spawn parameter can produce the section-3 model, so per the invocation's fallback the mismatch is recorded here prominently rather than redone. Seat B ran on Opus 4.8, one minor version below the nominal seat model; provider (Anthropic/Claude) is correct.
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP; account rejected the explicit `gpt-5.2-codex` override ("not supported when using Codex with a ChatGPT account"), so the account-default Codex GPT-5-class model was used. Self-reported `gpt-5-codex`. Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event: threadId 019fba8d-b53f-7b20-a578-040dc8c4f710.

Exchanges: 3/6

Exchange 1 — Seat A (Diagnostician), position: TWO layered CODE DEFECTS (not KB error — the KB is correct; not already-known — FINDINGS index empty, no rediscovery). (Primary, proximate freeze) The avatar is SURFING (`get_player_avatar().flags == AvatarFlags.Surfing`) at VERMILION_CITY (3,5) (20,24) facing Left; the tile in front, Sand (19,24) elev 3 non-surfable, is a dismount, not an ordinary step. Upstream `calculate_path` is surf-state-blind and emits a "1-step Left" that is a game-engine no-op while surfing (driver memory-state read: hold-Left-90f → 0 movement, while Up/Down/Right move on water). dexbot's held-direction walker holds Left with no per-step progress check → holds forever until the 30k-frame progress watchdog (runner.py:39) dumps the stall. Severity HIGH but narrow trigger surface (only navigate legs crossing a surf/shore dismount boundary); blast radius WIDE-latent = any objective routing across a coastal dismount, not just this dock. (Secondary, strategic gate) `planner.py:_graph_reachable` (43-66) gates `missing_catchable` solely on `_plan_via_graph`/`_walkable`, built from `_get_warp_edges` (navigation.py:51-74) = STATIC ROM warp tiles only; the Seagallop ferry is scripted (not a warp tile), so every Sevii map is flag-unlocked but permanently graph-unreachable → `missing_catchable()` EMPTY from Kanto, hiding the entire remaining unowned dex (all Sevii/Johto species). Structural, not acute. Confidence high on both as code-level facts (directly observed/reproduced); the causal link from the primary bug to THIS historical trip is inference-by-reproduction-signature (frozen-at-(20,24)-facing-Left matches exactly) — flagged as inference, not telemetry. Sources: memory-state reads of stall_b.ss1 (avatar flags/pos, tile geometry, party/bag, manual button-holds); runner.py:39; navigation.py:51-74,486-491; planner.py:43-66; data/dependencies.json:280 (Dunsparce "[Tri-Pass, sail]"); FINDINGS index empty. Seat A independently re-verified all cited line numbers against the repo.

Exchange 2 — Seat B (Fix Designer), position: Root causes confirmed at code level. (Defect-1 mechanism) Upstream `pokebot-gen3/modules/modes/util/walking.py` `navigate_to` is a bare `while True:` that recomputes `calculate_path` each iteration and, on `TimedOutTryingToReachWaypointError` with no active script, `pass`es and loops (~:377-405); its docstring (~:329-330) states it does NOT handle surf/land transitions. It never raises → dexbot's `except SkillError` re-plan handler at navigation.py:957 never fires → only the 30k watchdog ends it. PROPOSED FIX (written only, IN SCOPE as a patch): add helper `_walk_leg_guarded(inner, no_progress_frames=480)` in dexbot/navigation.py, modeled on `walk_carefully.settle()`'s stability sampling — pump the upstream generator, forward every yield, and if the avatar makes NO net progress for 480 *movable* frames (movable = OVERWORLD + controllable + no active script, so battles/warps/cutscenes never false-trip) `reset_held_buttons()` and raise SkillError; wrap ONLY the two `navigate_same_level` call sites (navigation.py:937 final walk, :954 warp leg). The raise is caught by the existing handler at :957 → clears cached_route, re-plans, and after `interruptions>30` (:963) fails the skill fast (bounded) so the caller defers — vs the silent 30k freeze. Optional Tier-2: on stall AND is_in_water, blind-step Up/Down/Right before re-planning. (Defect-2) FLAGGED AS A NEW SUBSYSTEM → **Fable escalation per §3** (protocol 41-42): model scripted transit as a first-class routing edge class (mirroring existing cut-edges), runtime-computed in `_plan_via_graph` (NOT persisted into nav_graph.json — the graph is epoch-keyed and ferry availability depends on a var outside the epoch key), gated on Tri-Pass + `MAP_SCENE_CINNABAR_ISLAND`; execution handled in navigate_to's loop via the existing but currently-UNWIRED story.py primitives `sail_to`/`sevii_accept`/`sevii_return_home` (STORY_SKILLS ~:2066-2095). Alternatives considered and rejected in writing — (immediate) edit upstream while-True (wide blast, ignores bogus-path root); Tier-1 re-plan only (loops to interruptions>30 — accepted as fail-fast, Tier-2 is the improvement); route surf via walk_carefully (wrong abstraction, still surf-blind); make calculate_path surf-aware (correct true root but upstream-scale, out of patch scope); _walk_out_of_pocket as primary (may not converge — kept as Tier-2). (strategic) hardcode _graph_reachable True (nav still can't sail → freeze on arrival); fake ROM warp tile (pollutes static model, landing is stateful); persist edges into nav_graph.json (epoch-key mismatch); planner-only sail-wrapper (leaves _shore_reachable/_nearest_mart empty). Regression surface: Defect-1 touches navigation.py only; baseline 74 passed/2 failed (BASELINE.md) must be unchanged; replay = load the surf savestate, drive navigate_to((3,5),(9,26)), assert SkillError within bounded frames + no 30k dump. Sources: walking.py:329-330,377-405; navigation.py:157-213(perform_cut),726-830(walk_carefully/settle),833-861(_walk_out_of_pocket),937/954/957/963; planner.py:43-149,488-510; story.py sail primitives; BASELINE.md; §3/§8.2/§9.

Exchange 3 — Seat C (Quality Gate, Codex gpt-5-codex), verdict: **PASS-WITH-CONCERNS** on the written proposal. Independently verified the load-bearing claims against the repo: walking.py:329 (docstring: does not handle surf/land transitions) and :377 (while-True catches TimedOut and passes when no script) — confirmed; map_path.py:945 treats water-elevation→land-elevation as passable WITHOUT checking avatar surf state — corroborates the proximate root; navigation.py:51 (_get_warp_edges = static ROM warp behaviours only); planner.py:43/79/488 (missing_catchable, _shore_reachable, _nearest_mart all depend on _plan_via_graph); story.py:2024/2410 (sevii_accept/sail_to exist, story/CLI-wired only, not nav/planner-wired). Axis findings — Classification: PASS (both defects supported). Citations: PASS-WITH-CONCERNS (load-bearing citations check out). Coherence: PASS-WITH-CONCERNS (guarded generator correctly converts the swallowed upstream loop into a SkillError the existing :957 handler catches; narrow, low-regression for the reproduced signature; Tier-2 blind-stepping must not ship without tight surf-boundary gating — it is fail-fast, not a true dismount fix). Alternatives: PASS (real alternatives rejected for defensible reasons; the deeper calculate_path-surf-aware root fix correctly identified as broader-blast upstream work). REFUTATION (specific, per §5): Seat B's claim that "every navigate-bearing skill funnels through navigate_to→navigate_same_level" is FALSE — story.py:138(≈145) and perform_cut (navigation.py:158≈173) call the upstream walker directly, bypassing the two wrapped sites; the coverage/skill-agnostic claim must be narrowed (does not break the immediate fix). Fable escalation for Defect-2: JUSTIFIED (scripted transit as a first-class routing edge is a new subsystem, not a local patch); concern for the escalated work — executable gating must be exact (Tri-Pass in bag + current harbor + current side of the ferry via MAP_SCENE_CINNABAR_ISLAND), since map data currently gates on BADGE08_GET with Tri-Pass only in notes. Defect-1 is SAFE TO SHIP INDEPENDENTLY of Defect-2 (stops this class of silent freeze without changing planner reachability; will not by itself make Sevii catchables visible — that remains the escalated strategic fix).

Driver verification of Seat C's §5 refutation (coverage-claim narrowing): grep of dexbot/ confirms Seat C is right and the gap is LARGER than cited — direct upstream-walker calls (`navigate_to` aliased `navigate_same_level`/`nav_same`/`_walk_level`) exist in boulders.py:282, gyms.py:115/121/225/236/291/497/519, safari.py:76, openings.py:45/51/83, perform_cut at navigation.py:173, and ~40 sites across story.py — NONE of which the proposed guard (navigation.py:937/954 only) covers. Impact assessment: the REPRODUCED Vermilion surf freeze occurs on dexbot's cross-map `navigate_to` final/warp legs, which ARE the wrapped sites, so Defect-1's fix does address the observed stall; but Seat B's "skill-agnostic / every navigate-bearing skill" blast-radius claim is overbroad and must be narrowed to "dexbot's cross-map navigate_to legs." Most unwrapped sites are interior story/gym/dungeon walks unlikely to cross a surf-dismount boundary, but safari.py/boulders.py/openings.py surf-adjacent legs remain latent. This refines (does not invalidate) the proposal; were it implemented, the guard should be applied at (or the surf-boundary check pushed into) a shared wrapper covering the direct-upstream call sites too.

Verdict: PASS-WITH-CONCERNS (on the written proposal, per dry-run rules — §9 replay/regression gate inapplicable; nothing implemented).
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction. No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): (i) Seat B's "every navigate-bearing skill funnels through navigate_to" coverage claim is overbroad — narrowed by driver verification above (the immediate fix still covers the reproduced stall); (ii) Tier-2 surf blind-step must ship only with tight surf-boundary gating; (iii) the escalated Defect-2 subsystem needs exact executable ferry gating (Tri-Pass + harbor + side), not the KB's BADGE08-only flag; (iv) Seat A's open question stands — with `missing_catchable()` empty from Kanto, the exact skill that produced 'stall_b' is not telemetry-recoverable (a heal/restock/patrol/assembly leg that surfed, not necessarily a catch), and a planner commit-guard (verify _plan_warp_route non-None before run_skill) was noted by Seat B as secondary belt-and-suspenders.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the fix remains a written proposal. No commit, no PARKED routing). Note: Defect-2 (Sevii ferry reachability) is the load-bearing blocker to dex COMPLETION — all ~28 remaining species are ferry-gated — but it is a Fable-escalation new-subsystem item, not implementable in this diagnose-only pass; were this live, Defect-1 would ship first (bounded freeze fix) and Defect-2 would open as escalated work, not a PARKED objective (parking the entire remaining dex is not warranted — the route exists in-game and story.py already has the sail primitives).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, TWO findings would record: (1) surfing avatar wedged by upstream held-direction walker on a surf-blind calculate_path dismount step → 30k stall, fixed by the _walk_leg_guarded per-leg progress guard; (2) scripted Seagallop ferry unrepresented in the warp graph → all Sevii/Johto species invisible to missing_catchable(), fixed by a runtime scripted-transit edge class wired to story.py sail primitives — sources as above).

Driver disclosures (side effects, all restored):
- Ran five headless read-only reproductions on stall_b.ss1 via the persistent-profile emulator (dexbot.emulator.setup_headless_emulator, DEXBOT_VIDEO=0, is_test_run=True): memory/tile reads, manual button-holds, missing_catchable()/accessible_maps()/_graph_reachable probes, upstream calculate_path probes, and two sandboxed navigate_to executions (to (3,5)(9,26) and to a Sevii tile). All telemetry/savestate writes were sandboxed: runner._events_path redirected to a scratch dir, runner._checkpoint_phase and runner._dump_stall monkeypatched to no-ops, and context.emulator.create_save_state stubbed — so no current_state.ss1, no fixtures/_stalls dump, and logs/skills.jsonl stayed absent.
- setup_headless_emulator created a `livingdex` profile dir (pokebot-gen3/profiles/livingdex/{metadata.yml,stats.db}; none existed before). Removed after the runs; no current_state.ss1 was written. Profile tree restored to as-found and verified (pokebot-gen3/profiles/ back to its 10 pre-existing loose config files + __pycache__). (Cf. memory `repro-writes-profile-dir`.)
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, data/, or any dexbot/ source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md. Evidence savestate stall_b.ss1 left byte-intact.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-003 — 2026-08-01T00:16:18Z
Trigger: §4.1 watchdog_stall — runner progress watchdog (dexbot/runner.py `_PROGRESS_BUDGET_FRAMES`=30_000, overworld budget) dumped a savestate after ~30k frames (~8 game-min) with the progress sample (map/coords/money/balls/game_state/party-HP) unchanged. Objective 'stall_c' (dry-run: historical, scrubbed name). Evidence: stall_c.ss1, stall_c_state.json; logs/skills.jsonl+progress.jsonl+STATUS.md absent (pre-run harness → which skill produced 'stall_c' is not telemetry-confirmed).

Seats: A=claude-sonnet-5  B=claude-opus-4-8[1m] (see Model check)  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet` (the spawn interface accepts only aliases, not full §3 IDs); self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). **VIOLATION (harness limitation, disclosed per invocation):** claude-opus-5 does not exist in this harness — the only Opus available is Opus 4.8. Spawned via alias `opus`; self-reported `claude-opus-4-8[1m]`. No spawn parameter can produce the section-3 model, so per the invocation's fallback the mismatch is recorded here prominently rather than redone. Seat B ran on Opus 4.8, one minor version below the nominal seat model; provider (Anthropic/Claude) is correct.
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP (account-default model; explicit gpt-5.2-codex override not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Self-reported `Codex GPT-5`. Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event: threadId 019fbaa9-a55b-7b82-9a50-7d638f7e1a43.

Exchanges: 3/6

Exchange 1 — Seat A (Diagnostician), position: **CODE DEFECT (navigation/control-flow) with an irreducible observation gap** — NOT KB error (geometry verified correct), NOT already-known (FINDINGS index empty). OBSERVED (high conf): the watchdog fired at 30k unchanged overworld frames and dumped at ROCKET_HIDEOUT_B1F (21,18) facing Down, mid-`clear_rocket_hideout` (Lift Key held, Giovanni undefeated, Silph Scope=0). Everything about *why* the controller sat there is INFERRED, and unusually hard to close: (item 1) B1F has ZERO omnidirectional forced-movement tiles (`navigation._has_spinners((1,42))`=False, navigation.py:812-830) → not a spin-maze fight; (item 2) the avatar is NOT trapped — from (21,18) calculate_path Up/Left/Right succeed, only Down→(21,19) has NO PATH (closed barrier (20-21,19-21), Grunt5 obj 5 present) and the avatar faces exactly that one blocked direction. Central tension: the dump geometry *looks* like `_exit_rocket_hideout`'s domain, but that fn only runs with Scope>0 (story.py:1227, 1382-1386) and Scope=0 here — per the flags the bot shouldn't be executing it. Two unverifiable live possibilities: (a) a caller reached (21,18) via a blind/direct upstream-navigate leg not flag-gated as assumed; (b) flags are downstream of the stall setup (speculative, no evidence). Concludes the exact wedge mechanism is **unrecoverable from the artifact** (a .ss1 restores game memory, not the live Python controller/held-button/generator state) — confidence MEDIUM at best on any specific mechanism. Severity HIGH (≈30k frames ~8 game-min + dump/recovery per hit; bounded by the watchdog but the objective — the Silph Scope — gates Pokémon Tower catches → Poké Flute → Snorlax → downstream dex per story.py:1191-1192). Blast radius: narrow (this objective's resume path) if barrier-pocket-specific; unknowable if generic controller-state corruption. Sources: runner.py:39/302/380-434; navigation.py:812-830; story.py:1131-1187/1190-1386; driver repros below; DRYRUN-002 cross-reference; FINDINGS index empty. Seat A re-verified cited line refs.

Exchange 2 — Seat B (Fix Designer), position: Root cause LOW confidence on mechanism, HIGH on the diagnostic gap; **declines a false-precision point-fix** for a trigger no reproduction reproduces (driver repro-6). Sourced systemic weakness: `clear_rocket_hideout` drives hideout legs through the RAW upstream `navigate_same_level` (imported story.py:1217), bypassing dexbot navigation.py's re-plan/blacklist/interruption guard (navigation.py:957-968) and any no-progress budget — those legs can burn frames on an upstream internal retry with nothing above them to fail fast. PROPOSED FIX (written only): **PRIMARY = observability, telemetry-only / non-behavioral** — enrich `_dump_stall` (dexbot/runner.py:92-114), before the `_log_event` at :107, with three additive fields: (i) `controller_stack=[c.__qualname__ for c in context.controller_stack]` (the datum computed live at runner.py:302 and discarded at dump — answers "which caller reached (21,18)"); (ii) `held_buttons` via `context.emulator.is_button_held(...)` (public API, libmgba.py:485-490) — "Down held" ⇒ held-direction bump loop, empty ⇒ wait-on-predicate wedge; (iii) `nav_intent` — a new navigation.py module global `_LAST_NAV` that `navigate_to` writes each leg (~:948), read best-effort (stale/empty for raw legs → controller_stack qualnames stay load-bearing). Squarely closes Seat A's "unrecoverable from artifact" gap for the NEXT occurrence with zero behavioral change. **SECONDARY = subordinate bounded per-leg guard** `_guarded(gen, budget=1500)` tracking `_progress_sample()[:2]`, raising SkillError after `budget` unchanged frames, wrapped around highest-risk raw legs (story.py:1319-1326; `_exit_rocket_hideout` :1178-1181) → converts a hypothetical 30k silent burn into a fast SkillError → skill deferral → re-plan. Honestly flagged: per repro-4/6 it would NOT have fired in any reproduction and may not catch THIS wedge — defense-in-depth, not a trigger fix, needing its own §9 replay; Seat B recommends Seat C be able to PASS the observability change INDEPENDENTLY of the guard. Alternatives considered and rejected in writing: (1) lower the global 30k watchdog — false positives (runner.py:43-44 comment: overworld treks/battles legitimately idle position for minutes); (2) guard only inside dexbot `navigate_to` legs (:934-954) — the hideout bypasses them via raw `navigate_same_level`; (3) point-fix `_exit_rocket_hideout`'s `y>=19` pocket heuristic → reachability-derived + assert-Grunt5-defeated (barrier removal sourced to story.py:1176, TRAINER_GRUNT_12) — rejected as the cause (that fn isn't running at Scope=0), reasonable hardening but speculative/uncausal here, defer until the enriched dump justifies it; (4) reconstruct-and-fix from the .ss1 — driver repro-6 proves the artifact doesn't carry the wedge → false precision. Regression surface: observability = logging-only + one additive global + one line in `navigate_to` → baseline 74 passed/2 failed (BASELINE.md) untouched (the 2 baseline failures are the unrelated Psyduck KB drift); the guard is behavioral and must clear §9 replay separately. Residual risk: enriched dump misses the wedge only if it clears within one 2000-frame sample window (a 30k freeze spans ~15 windows → near-certain capture); `held_buttons` reads empty during `reset_held_buttons` inter-tap cycling but still rules out held-walk; `nav_intent` may be stale for raw legs. Sources: runner.py:92-114/:302/:43-44; libmgba.py:485-490; navigation.py:864-968/:957-968; story.py:1217/1227/1319-1326/1382-1386/:1176; BASELINE.md; DRYRUN-002.

Exchange 3 — Seat C (Quality Gate, Codex GPT-5), verdict: **PASS-WITH-CONCERNS** on the written proposal. Independently verified against the repo: `_dump_stall` currently logs only `sample`/`script`/`state` (runner.py:92), `controller_stack` is computed live and discarded (runner.py:302), `is_button_held()` exists (libmgba.py:485), `_exit_rocket_hideout` is Scope-gated (story.py:1227/1382), the raw-upstream-walker bypass of dexbot's wrapper (navigation.py:864) is real. Axis findings — Classification: sound (not KB/collision/already-known; state is inside `clear_rocket_hideout` and the Scope=0 flags don't support `_exit_rocket_hideout` as the active site). Observability fix: coherent and properly scoped — "a real fix for the diagnostic gap, not for the underlying stall mechanism." Citations: PASS with one numeric concern. Alternatives: real and defensibly rejected. Coherence: sound and honest about scope. **REFUTATION (specific, per §5):** Seat B's "raw upstream `navigate_same_level` is called 75×" does NOT verify in this checkout — Seat C counts 35 actual `yield from navigate_same_level(...)` call sites and 46 textual occurrences (driver grep independently found 49 textual occurrences in story.py; all three disagree with 75). This refutes the NUMBER but not the load-bearing claim: the hideout does use raw upstream walker legs (story.py:1312-1326) bypassing dexbot's wrapper/replan (navigation.py:864). Explicit rulings requested by the driver: (a) it IS sound to PASS the observability change INDEPENDENTLY of the unimplemented guard, "as long as the PASS is explicitly for diagnostic instrumentation only" — the per-leg guard is behavioral, unproven against this event, and must require its own replay/regression evidence before acceptance; (b) Seat A's "unrecoverable from artifact" is a LEGITIMATE terminal conclusion, not a cop-out — the artifact lacks the live controller stack/held inputs/generator target, the current stall dump doesn't preserve them, and the repros show every forward replay escapes or fast-fails; bounded uncertainty after falsifying the plausible mechanisms is the correct terminal conclusion for a diagnose-only event.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9 replay/regression gate inapplicable; nothing implemented). The PASS attaches to the PRIMARY observability change as *diagnostic instrumentation only*; the SECONDARY per-leg guard is NOT accepted here — were this live it would require its own §9 replay/regression evidence before shipping.
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction. No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): (i) Seat B's "75×" raw-navigate count is wrong (actual ≈35 call sites / ≈46-49 textual occurrences) — a citation-precision defect that does not touch the load-bearing bypass claim; (ii) the SECONDARY bounded per-leg guard is behavioral, would not have fired in any reproduction (repro-4/6), and must pass its own §9 replay before acceptance — it is explicitly OUT of this PASS; (iii) the exact wedge mechanism is unrecoverable from this artifact (Seat A) — the primary fix targets that gap rather than the trigger; (iv) the objective label is scrubbed and telemetry-unconfirmed, and the anomalous Blastoise-L46 + Paras-L10 roster is an unexplained oddity noted but not causally linked to the stall (Seat A open-Q ii — no code fact ties party composition to this navigation freeze; not parked, left as an observation).
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written). Were this live: the PRIMARY observability enrichment of `_dump_stall` would ship first (non-behavioral, closes the diagnostic gap so the next occurrence of this class is root-causable from telemetry), and the SECONDARY guard would open as a separate, replay-gated change — NOT a PARKED objective (the objective is not blocked by a re-derivable dead end; the hideout route exists and completes in-game).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, a finding would record: a 30k-frame silent overworld freeze at ROCKET_HIDEOUT_B1F (21,18) whose exact wedge is NOT reconstructible from the stall `.ss1` because the dump omits the live controller_stack / held-button / nav-target state → fix = enrich `_dump_stall` with those fields (+ a subordinate replay-gated per-leg no-progress guard on the raw upstream hideout legs) — sources as above).

Driver disclosures (side effects, all restored):
- Ran three headless read-only reproductions on stall_c.ss1 via the persistent-profile emulator (dexbot.emulator.setup_headless_emulator, DEXBOT_VIDEO=0, is_test_run=True): (1) state/geometry reads + `_has_spinners`/`calculate_path` probes + a manual-driven upstream `navigate_to`/`walk_carefully`/dexbot `navigate_to` comparison; (2) collision/passability around the barrier, map-object presence, `_plan_warp_route` probes, and manual dexbot `navigate_to` drives; (3) raw-upstream-walker drives toward unreachable south tiles. All driven by hand via `context.emulator.run_single_frame()` (NOT `run_skill`), with `context.emulator.create_save_state` stubbed to a no-op as belt-and-suspenders. No telemetry (logs/skills.jsonl) or fixture (fixtures/_phases, fixtures/_stalls) writes occurred.
- setup_headless_emulator created a `livingdex` profile dir (pokebot-gen3/profiles/livingdex/{metadata.yml,stats.db}; none existed before — only the 10 pre-existing loose config files + __pycache__). Verified no current_state.ss1 was written, then removed the dir; pokebot-gen3/profiles/ restored to as-found. (Cf. memory `repro-writes-profile-dir`.)
- Evidence savestate stall_c.ss1 left byte-intact (md5 2c630c101067949e802a28919a5a5ed7 identical before and after all runs).
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, data/, or any dexbot/ source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-004 — 2026-08-01T00:40:09Z
Trigger: §4.1 watchdog_stall — the runner progress watchdog dumped a stall for objective 'stall_d' (dry-run: historical, scrubbed name). Unlike DRYRUN-001/002/003 (30k-frame OVERWORLD nav freezes), this fired under the **6,000-frame MENU budget** (`dexbot/runner.py:39-45` `_MENU_PROGRESS_BUDGET_FRAMES`, selected at :388-399 because the game state was non-overworld/non-battle): the sample (map/coords/money/balls/game_state/party-HP) sat unchanged in a shop menu. Evidence: stall_d.ss1, stall_d_state.json; logs/skills.jsonl+progress.jsonl+STATUS.md absent/empty (pre-run harness → the skill is identified from the savestate + code, not telemetry).

Seats: A=claude-sonnet-5  B=claude-opus-4-8[1m] (see Model check)  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet` (the spawn interface accepts only aliases, not full §3 IDs); self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). **VIOLATION (harness limitation, disclosed per invocation):** claude-opus-5 does not exist in this harness — the only Opus available is Opus 4.8. Spawned via alias `opus`; self-reported `claude-opus-4-8[1m]`. No spawn parameter can produce the section-3 model, so per the invocation's fallback the mismatch is recorded here prominently rather than redone. Seat B ran on Opus 4.8, one minor version below the nominal seat model; provider (Anthropic/Claude) is correct.
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP (account-default model; explicit gpt-5.x override not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Self-reported `Codex (GPT-5)`. Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event: threadId 019fbabf-e317-7e51-bd88-dfb4d97b6c51.

Exchanges: 3/6

Exchange 1 — Seat A (Diagnostician), position: **CODE DEFECT — "unbounded menu-actuation loop" class; the demonstrated instance is ALREADY REMEDIATED in the current repo; a residual instance of the same class remains unguarded.** NOT a KB error (stone prices/plans correct), NOT a planner deadlock, NOT a transient (deterministic wallet-vs-price arithmetic). The skill is `evolve_stones` (dexbot/evolution.py:297) at Celadon Dept Store 4F (MapFRLG (10,3), map_data.py:396). Driver read-only memory reads of stall_d.ss1 place the wedge INSIDE the FRLG buy menu: `get_game_state_symbol()="CB2_BUYMENU"`, which has no arm in `get_game_state`'s match (pokebot-gen3/modules/memory.py:257-319) and falls to `case _: GameState.UNKNOWN` (:318-319) — this is BY DESIGN and is why the tighter 6,000-frame menu budget applied, not a watchdog bug. Active task `Task_BuyHowManyDialogueHandleInput`, script `CeladonCity_DepartmentStore_4F_EventScript_Clerk`, buy-menu scroll pos 5 = Leaf Stone, quantity selector pinned ×01, money ₽3084, bag Leaf Stone 0. Shopping-list reconstruction (evolution.py:319-326): Gloom→Vileplume + Exeggcute→Exeggutor both need Leaf Stone, bag 0 → `to_buy={Leaf Stone:2}`; 2×₽2100=₽4200 > ₽3084. Root mechanism: FRLG clamps the buy-quantity selector at the wallet-affordable max, so a loop waiting for ×02 has an unreachable predicate (driver TEST 1: pressing Up 240f never exceeds ×01). BUT current code already fixes this: the wallet clamp at openings.py:176-177 (`quantity=min(quantity, money//price)`) reduces the ask to 1, and the quantity loop is the bounded `press_until` (runner.py:48-66, 600-frame budget, raises a *named* SkillError) — driver TEST 2 confirms the current-code path A-mashes the ×01 buy to completion (Leaf 0→1, ₽3084→984, returns to the buy menu). The comment at openings.py:173-175 documents this exact scenario verbatim ("the Leaf Stone wedge — asked for 2 × ₽2100 holding ₽3084; the selector pinned at ×01"), so the savestate provably predates the guard. RESIDUAL (same defect class, distinct sites): three UNBOUNDED `wait_until_task_is_active("...","A")` calls at openings.py:159/187/196, backed by the unbudgeted `while not task_is_active` loop at pokebot-gen3/modules/modes/util/tasks_scripts.py:11-21 — if any buy-flow task fails to activate (wrong counter tile, unexpected dialogue branch, declined/no-room prompt) they spin until the coarse 6k menu watchdog, which names only the skill, not the interaction. Severity: LOW/none for the demonstrated instance (remediated, replay-confirmed); latent-MEDIUM for the residuals (availability safe — bounded by the watchdog — but diagnostically blind). Blast radius: the shared `buy_items` helper. Confidence: HIGH on classification + current-code remediation; medium-high that the residual waits would ever actually fire. Sources: map_data.py:396; memory.py:257-319; runner.py:39-45/48-66/388-399; openings.py:132-205 (clamp 176-177, comment 173-175, waits 159/187/196, verification 202-205); tasks_scripts.py:11-21; evolution.py:297-346 (shopping math 319-326); stall_d_state.json; driver .ss1 memory inspection + TEST 1/TEST 2. FINDINGS index empty → no rediscovery.

Exchange 2 — Seat B (Fix Designer), position: Root cause CONFIRMED at code level (HIGH conf) = unbounded actuation against a game-clamped predicate; the demonstrated instance is remediated twice over (wallet clamp openings.py:176-177 + press_until 600-frame budget runner.py:48-66), concurs current code PASSES the demonstrated wedge (TEST 2). PROPOSED FIX (written only): **(a) For the demonstrated stall — NO further code change** (the path passes; re-fixing a passing path is a gratuitous edit). **(b) Residual defense-in-depth, precisely scoped:** add `wait_for_task(task_name, button, what, budget_frames=2400, interval=1)` in dexbot/runner.py mirroring press_until's contract (yields, presses `button` each `interval`, raises `SkillError(f"wait_for_task: {what} did not appear within {budget_frames} frames")` on expiry), and swap EXACTLY the three call sites openings.py:159/187/196 onto it. Budget 2,400f (~40s @60fps) sits above legitimate menu-open latency (clerk greeting → list → quantity dialog → post-purchase confirm/yes-no/"Here you are!", longest observed sub-1,000f) and strictly BELOW the 6,000f menu watchdog, so it fails FIRST with a precise message. Applied ONLY inside `buy_items` (a mart-only skill whose stable menu state `CB2_BUYMENU` has no legitimate 40s idle) → cannot false-trip battle intros / CHANGE_MAP / scripted cutscenes, which live in other helpers left untouched. Fixing the shared helper covers every caller in one change. **(c) Strategic follow-up:** the wallet-clamped multi-stone buy defers the 2nd evolution (after buying 1 Leaf Stone, evolution.py:337-343 evolves Gloom; Exeggcute hits `bag Leaf==0 → continue` at :338-339). Ruled SELF-HEALING — on the next planner re-queue `plans` rebuilds with Vileplume excluded, `to_buy={Leaf Stone:1}`, and (once money ≥ ₽2100) buys+evolves; the list drains one-per-run deterministically, the zero-money case is already guarded (evolution.py:326-327 raises "No money for evolution stones"), so NO guard is warranted — the only open assumption (the planner re-queues evolve_stones on remaining dex gaps) is flagged as a VERIFICATION item, not a code change. Alternatives considered and rejected in writing: (1) lower the 6k menu budget — coarse last-resort net, would false-trip legitimately-slow non-overworld states and still names only the skill; (2) do nothing — defensible for the demonstrated stall, insufficient for the residual; (3) point-fix one wait site — leaves two identical siblings; (4) blanket-wrap every `wait_until_task_is_active` or budget the upstream tasks_scripts helper — huge blast radius across battle/warp/cutscene/starter/PC modes, false-trips legit long waits, edits shared upstream (§9.3 risk); (5) planner-level affordability guard (queue evolve_stones only when money covers ALL stones) — pessimizes the dex; partial-buy-then-defer is correct for an unattended money-accumulating bot. Regression surface: baseline 74 passed/2 failed (BASELINE.md; the 2 = unrelated Psyduck pre-Brock planner drift); change = one new fn + three call-site swaps, happy path byte-identical behavior, no pytest references buy_items/buy_pokeballs/wait_until_task_is_active/press_until (mart flow is headless-replay-covered, not unit-tested). Fable escalation (§3): NO — patch to an existing subsystem, same class as the existing press_until; Seat A classified the event; no prior FAIL. Sources: openings.py:132-205; runner.py:48-66/39-45/388-399; evolution.py:20-32/297-346 (defer 338-339, zero-money guard 326-327); tasks_scripts.py:11-21; BASELINE.md; driver TEST 1/TEST 2.

Exchange 3 — Seat C (Quality Gate, Codex GPT-5), verdict: **PASS-WITH-CONCERNS** on the written proposal, explicitly agreeing that "no further code change for the demonstrated stall + scoped defense-in-depth for the three buy_items waits" is the right call. Independently verified the load-bearing citations against the repo: wallet clamp openings.py:176-177; the three unbudgeted waits openings.py:159/187/196; the unbudgeted upstream loop tasks_scripts.py:11-21; menu-vs-overworld watchdog selection runner.py:388-399; press_until runner.py:48-66; shopping math evolution.py:319-326; zero-money guard :326-327; defer path :337-339 — all correct. Axis findings — Classification: PASS (current code supports Seat A's read). Citations: PASS-WITH-CONCERNS (cited line numbers correct; memory-state facts sourced by the driver observations). Coherence: PASS (fix follows the root cause; clamp makes the predicate reachable and press_until fails precisely; the `wait_for_task(budget=2400)` wrapper applied only at the three buy_items waits is correctly scoped and won't false-trip battle/warp/cutscene waits; budget below the 6k watchdog and above normal shop-task latency is defensible). Alternatives: PASS (real and defensibly rejected, especially the global `wait_until_task_is_active` change, which has many upstream callers across game-corner/roamer-reset/starters/PC/item/shop helpers). **REFUTATION (specific, per §5):** Seat B's wording "Covers all callers (evolve_stones, buy_pokeballs) via the shared helper" is UNDERSTATED — `buy_items` has more dexbot callers than those two (evolution.py:197 Hyper-Potion restock, multiple gyms.py potion buys, planner.py ball restocks, story.py purchases). This does NOT invalidate the proposal (changing the helper-local waits still covers those callers) but the blast-radius statement should read "all `buy_items` callers, including evolve_stones and buy_pokeballs," not imply those are the only two. No forbidden scope fetched.

Driver verification of Seat C's §5 refutation (blast-radius wording): grep of dexbot/ confirms Seat C and the benefit is WIDER than Seat B stated — `buy_items(` has ~15 direct call sites (evolution.py:197/333, gyms.py:171/280/344/350/398/461/563, openings.py:209 [buy_pokeballs], planner.py:251/261, story.py:50/336/1506) and `buy_pokeballs` adds 4 more (catching.py:678, openings.py:254/270, planner.py:270). Every mart-purchase path in the bot routes through `buy_items`, so the shared-helper `wait_for_task` swap benefits ALL of them — this STRENGTHENS the proposal (wider benefit) and refines only the wording; the proposal's blast-radius note should be updated to "all buy_items callers" if implemented. Concern is a documentation-precision nit, not a defect in the fix.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9 replay/regression gate inapplicable; nothing implemented). The PASS attaches to: (i) the classification (already-remediated menu-wedge + residual unbounded waits); (ii) the decision to make NO code change for the demonstrated, replay-confirmed stall; and (iii) the scoped `wait_for_task` defense-in-depth at openings.py:159/187/196. Were this live, (ii) means the demonstrated objective needs no fix commit and the bot resumes; (iii) would ship as a separate, replay-gated hardening change (§9 replay: patch applied, drive a buy flow whose expected task never activates, assert a named SkillError within ~2,400 frames + no 6k dump; regression: still 74/2).
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction (both independently reached "already remediated + residual defense-in-depth"). No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): (i) Seat B's "covers all callers (evolve_stones, buy_pokeballs)" understates the ~19-site caller set — resolved by driver verification above (strengthens, does not invalidate); (ii) the strategic self-healing ruling rests on the unverified assumption that the planner re-queues evolve_stones on remaining dex gaps — flagged by Seat B as a verification item; were this live, confirm the planner behavior before relying on cross-run drain; (iii) the demonstrated fix is "no change" — the value of this event is the classification + the residual hardening, not a repair of the demonstrated (passing) path.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written). Were this live: NO fix commit for the demonstrated stall (current code passes it, replay-confirmed); the `wait_for_task` residual hardening would open as a separate replay-gated change — NOT a PARKED objective (the objective is not blocked by a re-derivable dead end; the stone purchase completes in-game under current code).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, a finding would record: `evolve_stones` wedged in the Celadon Dept-4F FRLG buy menu (game_state UNKNOWN=CB2_BUYMENU → 6k menu watchdog) because a pre-guard `buy_items` asked for 2 Leaf Stones (Gloom+Exeggcute plans) while holding ₽3084 and the FRLG quantity selector clamps at the ₽-affordable max (×01) → root-caused and already fixed by the wallet clamp openings.py:176-177 + bounded press_until; residual unbounded waits openings.py:159/187/196 hardened by a bounded `wait_for_task` helper — sources as above).

Driver disclosures (side effects, all restored):
- Ran two headless read-only reproductions on stall_d.ss1 via the persistent-profile emulator (dexbot.emulator.setup_headless_emulator, DEXBOT_VIDEO=0, is_test_run=True): (1) load + pure memory reads (game_state/callback2, active tasks, script stack, mart buyable items + scroll position + Task_BuyHowManyDialogueHandleInput data, money, pokedex owned, bag stones); (2) TEST 1 (press Up 240f — selector stays ×01) and TEST 2 (A-mash 400f — 1× Leaf Stone buy completes, money 3084→984), each from a fresh load_save_state of the evidence bytes. `context.emulator.create_save_state` was stubbed to a no-op for both runs; no telemetry (logs/skills.jsonl) or fixture (fixtures/_stalls, fixtures/_phases) writes occurred. No skill was driven via run_skill; frames were advanced by hand via run_single_frame() with button presses only. (Cf. memory `stall-ss1-omits-controller-state` — note this MENU stall, unlike the DRYRUN-001/002/003 nav freezes, IS reconstructible from the .ss1 because the menu/task state lives in game RAM.)
- setup_headless_emulator created a `livingdex` profile dir (pokebot-gen3/profiles/livingdex/{metadata.yml,stats.db}; none existed before — only the 10 pre-existing loose config files + __pycache__). Verified NO current_state.ss1 was written, then removed the dir; pokebot-gen3/profiles/ restored to as-found. (Cf. memory `repro-writes-profile-dir`.)
- Evidence savestate stall_d.ss1 left byte-intact (md5 edc25d758db55b1960383c22b911199b identical before and after all runs).
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, data/, or any dexbot/ source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-005 — 2026-08-01T01:21:06Z
Trigger: §4.1 watchdog_stall — the runner progress watchdog (dexbot/runner.py:388-436) dumped a savestate for objective 'stall_e' (dry-run: historical, scrubbed name). Unlike DRYRUN-001/002/003 (overworld NAV freezes) and DRYRUN-004 (MENU wedge), this fired on a **BATTLE** state under the 30k-frame budget (game_state BATTLE is grouped with OVERWORLD at runner.py:392-398, not the 6k menu budget): the progress sample (map/coords/money/balls/game_state/party-HP) sat unchanged inside a wild battle. Evidence: stall_e.ss1 (md5 89f17ade6c66e5a898732c7dc7daaa92), stall_e_state.json; logs/skills.jsonl+progress.jsonl+STATUS.md absent/empty (pre-run harness → the skill is identified from the savestate + code, not telemetry). State: DIGLETTS_CAVE_B1F (map (1,37)=map_data.py:121) overworld coords (42,39); party Snorlax L32 (74/147, 50.3%) / Blastoise L57 / Parasect L33 asleep / Marowak L33 (False Swipe) / Gloom L32 (Sleep Powder); badges 1-5, HM01+HM03; dex_owned 48; bag 1 Poké Ball + 21 Great Ball + 8 Super Potion (no Poké Doll/Fluffy Tail).

Seats: A=claude-sonnet-5  B=claude-opus-4-8[1m] (see Model check)  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet` (the spawn interface accepts only aliases, not full §3 IDs); self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). **VIOLATION (harness limitation, disclosed per invocation):** claude-opus-5 does not exist in this harness — the only Opus available is Opus 4.8. Spawned via alias `opus`; self-reported `claude-opus-4-8[1m]`. No spawn parameter can produce the section-3 model, so per the invocation's fallback the mismatch is recorded here prominently rather than redone. Seat B ran on Opus 4.8, one minor version below the nominal seat model; provider (Anthropic/Claude) is correct.
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP (account-default model; explicit gpt-5.x override not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Self-reported `Codex GPT-5-class`. Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event: threadId 019fbae3-e51c-74b1-8ef6-dc1f3a956038.

Exchanges: 3/6

Exchange 1 — Seat A (Diagnostician), position: **TWO CHAINED CODE DEFECTS; neither alone reproduces the stall.** NOT a KB error (geometry/species correct), NOT a planner deadlock, NOT a transient (deterministic given the precondition), NOT already-known (FINDINGS index empty). **(1) Proximate freeze — state-machine gap (HIGH conf, directly reproduced):** upstream `handle_battle_action_selection` (pokebot-gen3/modules/battle_action_selection.py:25-55) only calls `decide_turn` when battler-0's controller callback ∈ {HandleInputChooseAction, sub_802C098, bx_battle_menu_t6_2}; every other reachable value — including `CompleteOnInactiveTextPrinter2`, a legitimate state while a battle message sits undismissed — falls to the buttonless `else: yield; continue` (lines 53-55). GENERIC gap: it would freeze on ANY unhandled controller-callback value reached from turn-selection, not Arena-Trap-specific. Structural fact Seat A added: `handle_battle()` (battle_handler.py:54-82) has its OWN top-level `else` at 80-82 that DOES press B, but it only fires when the main callback is NOT HandleTurnActionSelectionState/sub_8012324 (battle_handler.py:56-57 delegates to the inner selection loop first) — so the generic "mash B when confused" net is structurally never reached for this (main-callback, controller-callback) combination. **(2) Trigger source — strategy-layer gap:** dexbot's `make_catch_decider` (dexbot/catching.py:410-423) returns the bare enum `BattleAction.RunAway` for every non-target wild encounter (line 421); the BattleListener maps that to `handle_battle(RunAwayStrategy())` (pokebot-gen3/modules/modes/_listeners.py:123-124,280-284); `RunAwayStrategy.decide_turn` (pokebot-gen3/modules/battle_strategies/run_away.py:17-35), finding no escape method and no usable damaging move, unconditionally attempts `TurnAction.run_away()` (a deliberate upstream design per its own line-34 comment) → against Arena Trap the engine prints "Wild DIGLETT prevents escape with ARENA TRAP!" via exactly the `CompleteOnInactiveTextPrinter2` state defect (1) cannot clear. Arena Trap forces escape_chance=0 unless Flying/Levitate (_util.py:76-82); no escape item in bag → `get_best_escape_method()`=None. Severity **HIGH** for instances hit (hard freeze, no self-clearing exit; totally deterministic given precondition: non-target encounter + trapping ability w/o Flying/Levitate + no escape item + active mon with no usable damaging move — not RNG-dependent). Blast radius **bounded-but-recurring**: any map where a trapping-ability wild mon co-locates with a differently-targeted species (Diglett's Cave Diglett/Dugtrio is the textbook case; mechanism is general to the ability, not the location). Availability risk is time/attempts not correctness — bounded by the runner's once-per-skill `battle_rescue` (dexbot/runner.py:400-430) which can B-mash a first stuck message then hand to HealingBattleStrategy → a terminating BotModeError for a hard 0% escape — PROVIDED defect (1) doesn't recur inside the rescue hand-off. Not a rediscovery (FINDINGS empty); the existing dexbot "Diglett Cave" guard (catching.py:381-386 for HealingBattleStrategy; 348-352 for WeakeningCatchStrategy) guards the DECISION layer of two strategies but NOT the un-hardened RunAwayStrategy path the listener always uses for non-target encounters, and the freeze MECHANISM is upstream in battle_action_selection.py regardless of strategy. Open questions to Seat B: Q1 can a nonzero-but-doomed escape_chance make the hardened strategies ALSO return a live run_away() into the wedge? Q2 fix layer (state-machine vs strategy)? Q3 confirm target species (inferred Dugtrio, since a Diglett target would THROW at odds≈0.5028≥0.5, not flee). Sources: all cited file:line refs opened and verified against the repo; driver memory-state reproductions of stall_e.ss1.

Exchange 2 — Seat B (Fix Designer), position: Root cause confirmed at code level. **Seat A Q1 resolved concretely (decides the fix layer):** Arena Trap makes `get_escape_chance()` return 0 outright at _util.py:77-82 BEFORE the speed/`escape_attempts` formula (_util.py:87-95) is reached; with escape_chance==0, `get_best_escape_method()` falls through to None (_util.py:97-133 for the no-escape-item case) → both "hardened" strategies (catching.py:348-352, 380-386) fall through cleanly; a live `run_away()` from `get_best_escape_method()` exists ONLY when NOT trapped (which consumes the turn → opponent acts → control re-enters at the recognized HandleInputChooseAction with observable HP/turn change → never the buttonless else). So a live run_away() and the trap-wedge are mutually exclusive by construction → **a strategy-layer fix is SUFFICIENT for the reproduced event**; the upstream state-machine gap is a general silent-spin landmine, not the thing feeding THIS freeze. **Critical correction (no Struggle):** `DefaultBattleStrategy.decide_turn` with no usable move (default.py:153-168) → `_handle_lead_cannot_battle` (225-239) → RAISES BotModeError (flee path 256; rotate path blocked by Arena Trap `can_switch()`=False _util.py:150-155 → 265; default 239) — it never selects Fight-with-0-PP, so the engine's Struggle fallback is never reached. The battle is genuinely unwinnable with this party state; the best achievable outcome is a **clean, named, immediate termination** (BotModeError → SkillError in <1 s) vs ~30k dead frames + dump today. **PROPOSED FIX (written only):** **PRIMARY (ships first; resolves the event; ZERO upstream edits):** the listener already honors a returned `BattleStrategy` object (_listeners.py:108-124) and `make_catch_decider` already uses that path for the target case (catching.py:420 returns WeakeningCatchStrategy()); change catching.py:421 from `return BattleAction.RunAway` to return a dexbot `class _EscapeAwareRunAwayStrategy(RunAwayStrategy)` whose `decide_turn`: if `get_best_escape_method()` not None → return it (escapable → flee, unchanged); elif `get_strongest_move_against(...)` not None → `TurnAction.use_move(...)` (trapped-but-can-fight → chip); else → `DefaultBattleStrategy.decide_turn(self, ...)` (→ BotModeError, fail fast, never the doomed run_away.py:35 Run). Subclassing RunAwayStrategy preserves `should_flee_after_faint`=True / `choose_new_lead_after_battle`=False (run_away.py:11-15). In the repro: escape None → no move → BotModeError; Run never chosen → CompleteOnInactiveTextPrinter2 never entered → wedge cannot form. Mirrors the target-case idiom exactly; blast radius confined to catch-mode non-target wild encounters. **SECONDARY (separable general hardening; upstream one-liner):** replace the buttonless else (battle_action_selection.py:53-55) with `context.emulator.press_button("B"); yield; continue`, restoring parity with the file's own ChooseMove branch (49-51) and handle_battle's outer else (battle_handler.py:80-82); naturally bounded by the enclosing while; a B-press advances CompleteOnInactiveTextPrinter2→HandleInputChooseAction in ~60 frames (driver repro). **HONESTY CAVEAT (self-reported):** SECONDARY alone does NOT resolve the repro — bare RunAwayStrategy re-chooses Run after each dismissal, a message↔action-select livelock (a pre-emptive trap block does not consume the turn, so HP/pos/turn never change) → same 30k watchdog; its value is orthogonal (converts the silent-freeze CLASS into self-dismiss/fast-fail for any pending message at turn-selection, incl. inside the runner rescue hand-off — the only place that net can live, since the dispatch loop is upstream). Tighter §9.3 variant if unconditional B is too broad: gate to `get_battle_controller_callback(0).startswith("CompleteOn")`. **Alternatives considered and rejected in writing:** (a) edit upstream run_away.py:35 directly — shared upstream semantics used by every run-away mode (§9.3); dexbot-local override at our own call site achieves the identical effect for our encounters only; (b) strategy-only, no state-machine fix — this IS the PRIMARY, sufficient for the event, only shortfall is leaving the silent-spin class latent (acceptable fallback if the upstream edit is refused); (c) state-machine-only — livelocks on this trigger, insufficient alone; (d) lower the 30k watchdog budget — wrong layer, false positives (runner.py:44 notes battles legitimately idle for minutes); (e) runner-level generic stuck-message dismisser — reactive/coarse/once-per-skill latch, duplicates handle_battle; (f) do-nothing / rely on battle_rescue — ~30k wasted frames + once-per-skill latch ("poisons a whole catch queue"). **Regression surface:** PRIMARY touches catching.py only (non-target catch encounters); no dexbot test asserts the enum return (driver grep of tests/ empty); SECONDARY has broad upstream surface but is a minimal no-op→B-press change mirroring existing idioms (tighter CompleteOn* variant available if any battle-flow test regresses); baseline BASELINE.md 74 passed/2 failed (Psyduck pre-Brock drift, untouched) expected to hold. **Fable escalation (§3): NO** — both are patches to existing subsystems (PRIMARY extends the existing dexbot.catching strategy pattern; SECONDARY is a one-line edit to an existing upstream fn), no new subsystem. **Order:** PRIMARY first (necessary & sufficient for this event); SECONDARY second (defense-in-depth, neither necessary nor sufficient alone). If forced to ship one: PRIMARY. Sources: every cited line verified against the files this session (the trap→0 early-return _util.py:77-82, the None fall-through :97-133, the terminal doomed Run run_away.py:35, the buttonless else + B-pressing sibling battle_action_selection.py:42-55, the unreachable outer B-fallback battle_handler.py:56-57/80-82, the raise-not-Struggle path default.py:153-168/225-256, the listener BattleStrategy contract _listeners.py:108-124/280-284, the catch decider catching.py:410-423); driver reproduction taken as authoritative for the Snore-non-usable move fact.

Exchange 3 — Seat C (Quality Gate, Codex GPT-5-class), verdict: **PASS-WITH-CONCERNS** on the written proposal. Independently verified the load-bearing claims against the repo. Axis findings — **Classification: PASS** (two-defect classification checks out: battle_action_selection.py:25-55 delegates turn selection but buttonlessly yields at 53-55; battle_handler.py:54-82's B fallback is structurally unreachable while the main callback remains HandleTurnActionSelectionState). **Coherence: PASS** (catching.py:410-421 returns bare BattleAction.RunAway for non-targets; _listeners.py:108-124 honors a returned BattleStrategy BEFORE enum handling and :280-284 maps the enum to bare RunAwayStrategy() — the PRIMARY fix is correctly wired and sufficient for the reproduced event). **Alternatives: PASS** (secondary-only B-mash would dismiss the text but re-enter action choice where bare RunAwayStrategy can choose run_away.py:35 again → message/action livelock; PRIMARY-first ordering justified). **Citations: PASS-WITH-CONCERN** — load-bearing citations check out; **REFUTATION (specific, §5):** Seat B's "with escape_chance==0, `get_best_escape_method()` returns None" is OVERBROAD — _util.py:106-112 can still return Poké Doll/Fluffy Tail when escape_chance<0.9; for THIS event the bag has no escape item so the event conclusion remains valid (and the PRIMARY fix consults get_best_escape_method() directly, so it would correctly USE such an item if present → the concern refines the diagnosis wording, not the fix). Seat C additionally VERIFIED from allowed pret decomp (§10) that a trap-blocked Run does NOT consume the turn — `IsRunningFromBattleImpossible()` rejects Arena Trap before confirming Run and returns to action choice (pret/pokefirered src/battle_main.c:2852-2908, 3090-3097) — confirming Seat B's SECONDARY-alone livelock claim. Also confirmed the raise-not-Struggle path with the config default (dexbot's `lead_cannot_battle_action="flee"` at dexbot/emulator.py:49, schema default schemas_v1.py:22 → BotModeError at default.py:256). Minor Seat A/B citation-labeling nit noted (catching.py:381-386 is HealingBattleStrategy; WeakeningCatchStrategy's escape branch is 348-352 — both were in fact cited correctly). Did not fetch the forbidden scope.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9 replay/regression gate inapplicable; nothing implemented). The PASS attaches to: (i) the two-defect classification; (ii) the PRIMARY dexbot-local `_EscapeAwareRunAwayStrategy` as the necessary-and-sufficient fix for the reproduced event (clean BotModeError, wedge cannot form); (iii) the SECONDARY upstream one-line B-press as separable defense-in-depth for the silent-spin CLASS (explicitly NOT sufficient alone — it livelocks on this trigger). Were this live: PRIMARY would ship first as the event's fix and the bot resume; SECONDARY would open as a separate, replay-gated upstream hardening change (§9 replay: drive a non-target Arena-Trap encounter, assert a named SkillError within bounded frames + no 30k dump; regression: still 74/2).
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction (A classified, B designed, both independently reached the two-layer picture). No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): (i) Seat C's §5 refutation — Seat B's "escape_chance==0 → get_best_escape_method None" is overbroad (a Poké Doll/Fluffy Tail can still return < 0.9 escape_chance); for this event the bag is empty of escape items so the conclusion holds, and the PRIMARY fix correctly consults get_best_escape_method() so it would use such an item if present — refines the diagnosis wording, not the fix; (ii) the SECONDARY upstream B-press is behavioral, has broad shared-upstream blast radius (§9.3), and would require its own §9 replay before shipping — it is explicitly OUT of the PRIMARY's sufficiency and does NOT resolve the repro alone (livelock, confirmed from pret decomp); (iii) target species is inferred (Dugtrio), telemetry-unconfirmed (scrubbed objective) — the fix is scoped to be correct for ANY non-Diglett target since the defect is general; (iv) Seat A's structural observation stands — the runner's once-per-skill battle_rescue can mask a FIRST occurrence into a clean fail but its `battle_rescued` latch leaves a second trapped encounter in the same skill unprotected.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written). Were this live: the PRIMARY dexbot-local escape-aware flee strategy (catching.py:421) would ship as the event's fix commit (referencing the event ID) and the bot resume after the BotModeError-driven clean deferral; the SECONDARY upstream battle_action_selection.py:53-55 B-press would open as a separate replay-gated hardening change — NOT a PARKED objective (the objective is not blocked by a re-derivable dead end; the target is obtainable in-game and the fix makes the non-target encounter fail fast so the catch loop re-prepares and retries).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, a finding would record: a wild non-target encounter in DIGLETTS_CAVE_B1F silently froze the battle at HandleTurnActionSelectionState because dexbot routed it to bare upstream RunAwayStrategy, which attempts an escape that Arena Trap permanently blocks, printing "prevents escape" — a message the upstream `handle_battle_action_selection` else-branch never dismisses (no button press) → 30k no-progress watchdog stall; fixed PRIMARY by a dexbot-local escape-aware flee strategy that fails fast with BotModeError when trapped-and-no-move, and SECONDARY by an upstream B-press that dismisses pending turn-selection messages — sources as above, plus pret src/battle_main.c:2852-2908 for the no-turn-consumed trap block).

Driver disclosures (side effects, all restored):
- Ran headless read-only reproductions on stall_e.ss1 via the persistent-profile emulator (dexbot.emulator.setup_headless_emulator, DEXBOT_VIDEO=0, is_test_run=True): (1) pure battle-RAM reads (get_battle_state opponent/own battlers, moves+PP, ability, status; get_main_battle_callback / get_battle_controller_callback; on-screen text buffers gDisplayedStringBattle/gStringVar4; party/bag); (2) driving modules.battle_handler.handle_battle with WeakeningCatchStrategy and make_healing_battle_strategy after reclaiming context.bot_mode from the default "Manual" — both spin 20,000+ frames WITHOUT calling decide_turn (proving the wedge is upstream of the strategy in handle_battle_action_selection's else branch); (3) direct decide_turn / get_best_escape_method / catch-odds probes (one_turn_catch_chance≈0.5028, get_best_escape_method None, healing strategy raises BotModeError); (4) callback evolution under no-input vs A-mash vs B-mash (no-input pinned at CompleteOnInactiveTextPrinter2 for 20k frames; B-mash reaches HandleInputChooseAction in ~60 frames). All frames advanced by hand via context.emulator.run_single_frame() (NOT run_skill); `context.emulator.create_save_state` stubbed to a no-op for every run → no current_state.ss1, no fixtures/_stalls or _phases dumps, logs/skills.jsonl stayed absent. (Cf. memory `stall-ss1-omits-controller-state`: this is a BATTLE stall, and — like the DRYRUN-004 menu wedge and unlike the DRYRUN-001/002/003 nav freezes — it IS reconstructible from the .ss1 because the battle state (callbacks, battler structs, text buffer) lives in game RAM.)
- setup_headless_emulator created a `livingdex` profile dir (pokebot-gen3/profiles/livingdex/{metadata.yml,stats.db}; none existed before — only the 10 pre-existing loose config files + __pycache__). Verified NO current_state.ss1 was written, then removed the dir; pokebot-gen3/profiles/ restored to as-found. (Cf. memory `repro-writes-profile-dir`.)
- Evidence savestate stall_e.ss1 left byte-intact (md5 89f17ade6c66e5a898732c7dc7daaa92 identical before and after all runs).
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, data/, or any dexbot/ or pokebot-gen3/ source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-006 — 2026-08-01T02:00:32Z
Trigger: §4.1 watchdog_stall — the runner progress watchdog (dexbot/runner.py:39 `_PROGRESS_BUDGET_FRAMES`=30_000, OVERWORLD budget selected at :388-398 because game_state=OVERWORLD) dumped a savestate for objective 'stall_f' (dry-run: historical, scrubbed name) after ~30k frames with the progress sample (map/coords/money/balls/game_state/party-HP) unchanged. Unlike DRYRUN-001/002/003 (silent NAV-controller freezes) this OVERWORLD stall is fully RECONSTRUCTIBLE from the .ss1 because the wedge lives in game RAM (an undismissed field-script message box), like the DRYRUN-004 menu and DRYRUN-005 battle wedges. Evidence: stall_f.ss1 (md5 4fe7b7ea7089d0c26a7240f28ecc96c9), stall_f_state.json; logs/skills.jsonl+progress.jsonl+STATUS.md absent/empty (pre-run harness → the skill is identified from the savestate + code, not telemetry). State: VICTORY_ROAD_3F (map (1,41)=map_data.py:125) coords (33,19) facing Up; party Blastoise L77 / Parasect L34 / Zapdos L50 / Arbok L44 / Primeape L42 / Machoke L28; all 8 badges; GOT_HM04 (Strength) true, HM02(Fly)/HM05(Flash) false; dex_owned 100 / seen 129; money ₽38024.

Seats: A=claude-sonnet-5  B=claude-opus-4-8[1m] (see Model check)  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet` (the spawn interface accepts only aliases, not full §3 IDs); self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). **VIOLATION (harness limitation, disclosed per invocation):** claude-opus-5 does not exist in this harness — the only Opus available is Opus 4.8. Spawned via alias `opus`; self-reported `claude-opus-4-8[1m]`. No spawn parameter can produce the section-3 model, so per the invocation's fallback the mismatch is recorded here prominently rather than redone. Seat B ran on Opus 4.8, one minor version below the nominal seat model; provider (Anthropic/Claude) is correct.
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP (account-default model; explicit gpt-5.x override not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Self-reported `Codex, GPT-5-class coding agent`. Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event: threadId 019fbb08-676f-7492-861b-33b8066e7b46.

Exchanges: 3/6

Exchange 1 — Seat A (Diagnostician), position: **CODE DEFECT (control-flow), HIGH severity, defect-CLASS with ≥3 live sites.** NOT a KB error (geometry/flags correct), NOT a planner deadlock, NOT a transient (deterministic given SYS_USE_STRENGTH set), NOT already-known (FINDINGS index empty). Driver read-only RAM reads of stall_f.ss1: an overworld field script is pinned OPEN — `get_global_script_context().stack = ['EventScript_AlreadyUsedStrength','Std_MsgboxSign']`, on-screen message "STRENGTH made it possible to move boulders around."; `get_event_flag("SYS_USE_STRENGTH")=True`; boulder unpushed at (33,18); (34,18) is a Fall-Warp hole; VR scene vars MAP_SCENE_VICTORY_ROAD_1F/_2F_BOULDER1/_3F=100, **_2F_BOULDER2=0** (only Button 4 remains). Root mechanism (read + directly reproduced against the live tree): `dexbot/boulders.py:187-223` `activate_strength` iterates stand-candidates starting `((0,1),"Up")` → navigates to `(px,py+1)=(33,19)` facing Up, presses A on the boulder (boulders.py:198 order → exactly the observed geometry), then at :214-220 sees `script.is_active` True and calls `wait_for_yes_no_question("Yes")` (pokebot-gen3/modules/modes/util/tasks_scripts.py:80-107, a `while True:` that exits ONLY on `Task_YesNoMenu_HandleInput` or a specific multichoice task). With SYS_USE_STRENGTH already set, the A-press yields the plain `Std_MsgboxSign` sign, which spawns NEITHER task → structurally non-terminating loop → 30k watchdog dump (this is a non-terminating branch, not a race). Severity HIGH: an indefinite wedge on the MANDATORY Victory Road → E4 gate (GOAL.md success = autonomous progress). Blast radius: defect CLASS — (1) demonstrated `activate_strength`; (2) verbatim duplicate guard `dexbot/story.py:2234-2245` `_vr_push_left_until` on the live btn4_push path (story.py:2350-2357); (3) `push_boulder_sequence` default `activate=True` at btn1/2/3 (story.py:2312/2319/2330) and `run_boulder_puzzle` default `activate=True` (story.py:2125 F1, :2143 F2; boulders.py:329-333), any of which hangs if re-entered with Strength already on (crash-resume mid-floor, or run_boulder_puzzle's per-switch re-solve loop boulders.py:336-354). Note `btn4_drop` (story.py:2346) already uses `activate=False` with a comment documenting the hang → the DEMONSTRATED (33,18) instance is guarded in the current mainline, but the class is live. Confidence HIGH on mechanism (reproduced: driving `activate_strength(F3,(33,18))` hangs 2500+ frames with the sign open); MEDIUM on the exact historical provenance of the fixture (git is a single squashed import commit fc6352b; the current wired path uses activate=False, so the fixture was produced by an earlier/greedy path — the vestigial `_VR_SWITCHES`+run_boulder_puzzle design in the comment block story.py:2149-2168 is the likely origin). Open questions to Seat B: (1) fix the guard itself (distinguish/dismiss the "already used" sign from a real Yes/No prompt); (2) collapse `_vr_push_left_until` onto the fixed primitive to kill the duplicate; (3) add a bounded frame budget (press_until convention runner.py:48-66) so any other variant fails fast with a named SkillError. FINDINGS index empty → no rediscovery.

Exchange 2 — Seat B (Fix Designer), position: Root cause confirmed and sharpened (engaging Seat A, not restating). **The gate is a false positive:** activation gates on the COARSE one-bit `ScriptContext.is_active` (`tasks.py:101-102`, `= self._data[1] != 0`, True for ANY field script — the "Use STRENGTH? Yes/No" confirm AND the plain sign alike) instead of the game's OWN per-map branch predicate `FLAG_SYS_USE_STRENGTH` (pret pokefirered flags.h 0x805 = "Strength has already been used on this map"); it then hands off to an UNBOUNDED Yes/No waiter (no frame budget → violates the press_until convention runner.py:48-66). Two compounding defects: wrong discriminator + unbounded wait. **Correction to Seat A:** `FLAG_SYS_USE_STRENGTH` is per-map and cleared on the F3→F2 Fall-Warp reload, so `_vr_push_left_until` likely sees it CLEAR on the strict mainline → a LATENT duplicate, not a certain live second hang; the fix makes it moot. **PRIMARY FIX (written only, dexbot-local, minimal blast radius):** new NON-navigating helper `confirm_strength_if_prompted()` in dexbot/boulders.py — `if get_event_flag("SYS_USE_STRENGTH"): return` (mirror the game's branch, never open the sign) else press A and run a BOUNDED loop (120 frames) that answers Yes when `Task_YesNoMenu_HandleInput` appears or breaks when the flag latches, then `wait_for_player_avatar_to_be_controllable("B")`. `activate_strength` (boulders.py:187-223) gains a top-level `if get_event_flag("SYS_USE_STRENGTH"): return` early-out and its press-A/`is_active` block (207-220) is replaced by a call to the helper; `_vr_push_left_until` (story.py:2234-2245) collapses onto the same helper (keeps its own face-Left; no navigation → no ladder re-warp). Covers all `activate=True` sites. Early-return is position-safe because every caller re-navigates via `_do_push`→`navigate_same_level` (boulders.py:282/365) — verified against all 3 call sites; leaves btn4_drop's `activate=False` as-is (minimal diff; follow-up cleanup note). **Alternatives considered and rejected in writing:** (1) match script-stack symbol names — brittle/racy vs the canonical flag; (2) bound the Yes/No wait only, no flag gate — still opens the sign, wastes ~120f + a dismiss round-trip; (3) fix upstream `wait_for_yes_no_question` — §9.3 wide blast radius across shared upstream modes (PkmnCenter confirm etc.); (4) discriminate via `is_waiting_for_input()` — True for BOTH the sign and the pre-Yes/No text (tasks.py:199-215), cannot distinguish; (5) push `activate=False` onto all callers — scatters the invariant, fails crash-resume, leaves the run_boulder_puzzle re-solve loop exposed. **Fable escalation (§3): NO** — Seat A classified the event, zero prior FAILs, and the fix is a localized guard + one extracted helper WITHIN the existing boulders module (not a new subsystem). Regression surface: grep of tests/ finds nothing exercising activate_strength/wait_for_yes_no/boulder/push_boulder/_vr_push paths; all touched imports are function-local (no import-time surface); baseline BASELINE.md 74 passed/2 failed (Psyduck pre-Brock KB drift, untouched) expected UNCHANGED. Residual risk: the 120-frame prompt-appearance budget (mitigated — the Yes/No task spawns within a few frames; original was 90f); trusts FLAG⟺push-mechanic coupling (same field-move script sets both → desync effectively impossible). Explicitly OUT OF SCOPE: the driver's secondary observation (navigate_same_level not cleanly terminating on the y=18 one-way/elevation row during the btn4 push) = the DRYRUN-002 upstream bare-`while True` walker-fragility class, a separate defect with its own proposed guard. Live-replay assertions were this real (§9): drive with the flag SET → generator exhausts <200f AND the sign is never opened (pre-fix hangs 2500+f); drive with the flag CLEAR → flag flips + a subsequent _do_push moves the boulder; pytest 74/2; diff confined to boulders.py + story.py, no upstream/protocol/telemetry edits. Sources: memory-state repro; pret flags.h 0x805; in-repo memory.py:336 get_event_flag, tasks.py:101-102/199-215, tasks_scripts.py:71-100; frlg.txt:743.

Exchange 3 — Seat C (Quality Gate, Codex GPT-5-class), verdict: **PASS-WITH-CONCERNS** on the written proposal. Independently verified the load-bearing citations against the repo. **Classification sound:** `activate_strength` presses A, treats any active script as a Yes/No prompt, then calls the unbounded waiter (dexbot/boulders.py:207-218); the waiter loops until `Task_YesNoMenu_HandleInput`/qualifying multichoice appears (tasks_scripts.py:80-107); `ScriptContext.is_active` is only `_data[1] != 0` so it cannot distinguish the "AlreadyUsedStrength" sign from the real prompt (tasks.py:101-102). **Diagnosis→fix coherent:** gating on `get_event_flag("SYS_USE_STRENGTH")` is appropriate — flag resolves at frlg.txt:743 and `get_event_flag` reads the save bit at memory.py:336-343; collapsing the `_vr_push_left_until` duplicate (story.py:2234-2245) is justified given btn4_drop is already `activate=False` with the documented hang (story.py:2340-2346); the listed `activate=True` sites are present (story.py:2125/2143/2312/2319/2330) and both boulder runners funnel through `activate_strength` (boulders.py:305, 333). **Seat B's per-map correction is CORRECT and independently confirmed from allowed pret sources:** `FLAG_SYS_USE_STRENGTH` is a temporary system flag cleared by `ClearTempFieldEventData`, which `LoadMapFromWarp` invokes; VictoryRoad_3F has a warp at (34,18) to VictoryRoad_2F (pret include/constants/flags.h:1304-1312, src/event_data.c:44-50, src/overworld.c:742-750, data/maps/VictoryRoad_3F/map.json:217-222). **Alternatives:** real and defensibly rejected; no substantive counterexample to the fix found. **CONCERN (specific, §5):** Seat B's citation trail for the per-map/fall-warp correction was too compressed — the claim is correct but should have cited the clear routine (`ClearTempFieldEventData`) and the VictoryRoad_3F warp definition, not only "pret flags.h 0x805"; Seat C supplied the missing citations above. Did not fetch the forbidden scope.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9 replay/regression gate inapplicable; nothing implemented). The PASS attaches to: (i) the classification (CODE DEFECT / HIGH / defect-CLASS: unconditional `wait_for_yes_no_question` after a boulder A-press that yields a plain sign when Strength is already active); (ii) the PRIMARY dexbot-local fix — flag-gated, bounded `confirm_strength_if_prompted()` helper with `activate_strength` and `_vr_push_left_until` collapsed onto it, covering all activate=True sites; (iii) the no-Fable-escalation and no-code-change-to-btn4_drop calls. Were this live: the fix would ship as the event's commit (referencing the event ID) with §9 replay = drive activation with SYS_USE_STRENGTH set, assert the generator exhausts <200 frames and the sign is never opened + regression 74/2; the DRYRUN-002-class btn4-push walker fragility would remain a SEPARATE replay-gated change, not folded in.
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction (A classified, B designed and refined A's blast-radius nuance, both converged on the flag-gated helper). No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): (i) Seat C's §5 concern — Seat B's per-map/fall-warp correction, though correct, was under-cited; Seat C supplied `ClearTempFieldEventData`/`LoadMapFromWarp`/VictoryRoad_3F warp (34,18) citations; (ii) Seat B's own residual-risk flags (120-frame prompt-appearance budget; FLAG⟺mechanic coupling trust) — both judged low-risk; (iii) the fixture's exact historical provenance is telemetry-unconfirmed (scrubbed objective; current mainline btn4_drop uses activate=False) — Seat A MEDIUM confidence, does not change the fix target; (iv) the DRYRUN-002-class navigate_same_level fragility on the VR-3F y=18 one-way row is a SEPARATE latent concern surfaced by the driver's (non-load-bearing, hand-drive-imperfect) secondary observation, explicitly out of this proposal's scope.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written. No commit, no PARKED routing). Were this live: the flag-gated `confirm_strength_if_prompted()` fix would ship first as the event's fix commit and the bot resume; NOT a PARKED objective (the Victory Road route exists and completes in-game; the objective is not blocked by a re-derivable dead end).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, a finding would record: a wild-free OVERWORLD silent freeze at VICTORY_ROAD_3F (33,19) because `dexbot.boulders.activate_strength` (and its duplicate `story._vr_push_left_until`) press A on a Strength boulder and then unconditionally `wait_for_yes_no_question("Yes")`, but with `SYS_USE_STRENGTH` already set the A-press yields the plain `EventScript_AlreadyUsedStrength` sign — which never spawns a Yes/No task → 30k-frame watchdog stall; fixed by gating activation on `get_event_flag("SYS_USE_STRENGTH")` via a bounded, non-navigating `confirm_strength_if_prompted()` helper that both the primitive and the duplicate call — sources: boulders.py:187-223, story.py:2234-2245/2340-2346, tasks_scripts.py:80-107, tasks.py:101-102/199-215, memory.py:336-343, frlg.txt:743, pret flags.h:1304-1312 / event_data.c:44-50 / overworld.c:742-750 / VictoryRoad_3F map.json:217-222).

Driver disclosures (side effects, all restored):
- Ran six headless read-only reproductions on stall_f.ss1 via the persistent-profile emulator (dexbot.emulator.setup_headless_emulator, DEXBOT_VIDEO=0, is_test_run=True): (1) pure RAM reads (player avatar coords/facing/flags, game_state, SYS_USE_STRENGTH flag, VR scene vars, _initial_boulders + get_map_objects current_coords, script context, tile collision/type/warp grid around (33,19)); (2) message-buffer read (gStringVar4) + input-response tests (no-input 600f keeps the sign pinned; B dismisses it; A-mash; Up/Right after B); (3) driving `activate_strength(F3,(33,18))` → HANG reproduced (2500+f, sign open, pinned at (33,19)); (4) driving current btn4 `push_boulder_sequence(...,activate=False)` from the poisoned (message-open) load; (5) fair test from a B-dismissed state; (6) `navigate_same_level` termination probes to (32,19)/(31,18)/(32,18)/(33,17). All frames advanced by hand via `context.emulator.run_single_frame()` (NOT run_skill); `context.emulator.create_save_state` stubbed to a no-op for every run → no `current_state.ss1`, no fixtures/_stalls or _phases dumps, logs/skills.jsonl stayed absent. (Cf. memory `stall-ss1-omits-controller-state`: this OVERWORLD stall — like the DRYRUN-004 menu and DRYRUN-005 battle wedges and UNLIKE the DRYRUN-001/002/003 nav freezes — IS reconstructible from the .ss1 because the wedge is an undismissed field-script message box living in game RAM; the secondary navigate_same_level fragility is a hand-drive-imperfect observation flagged non-load-bearing.)
- setup_headless_emulator created a `livingdex` profile dir (pokebot-gen3/profiles/livingdex/{metadata.yml,stats.db}; none existed before — only the 10 pre-existing loose config files + __pycache__). Verified NO current_state.ss1 was written, then removed the dir; pokebot-gen3/profiles/ restored to as-found. (Cf. memory `repro-writes-profile-dir`.)
- Evidence savestate stall_f.ss1 left byte-intact (md5 4fe7b7ea7089d0c26a7240f28ecc96c9 identical before and after all runs).
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, data/, or any dexbot/ or pokebot-gen3/ source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-007 — 2026-08-01T14:19:38Z
Trigger: §4.1 watchdog_stall — the runner progress watchdog (`dexbot/runner.py:39` `_PROGRESS_BUDGET_FRAMES`=30_000, OVERWORLD budget selected at :415-426; sampled every 2,000 frames at :407; dump+raise at :458-463) dumped a savestate for objective 'stall_g' (dry-run: historical, scrubbed name) with the progress sample `((3, 5), (20, 24), 136, 10, 0, 7, (286, 59, 251, 328, 77, 64))` = map/coords/money ₽136/Poké Balls 10/Great Balls 0/game_state 7 OVERWORLD/party-HP. **Unlike every prior dry-run this event has FOUR dumps of the same wedge** (`stall_g_prior1/2/3.ss1` + `stall_g.ss1`, md5 42debf71…/fd0bc9c4…/9a7c5804…/1e97e392…), whose emulator frame counts are 1,615,519 → 1,647,519 → 1,679,519 → 1,711,519 — **exactly 32,000 apart** (30,000-frame budget + 2,000-frame sample granularity) with zero intervening emulated frames, plus a harness-synthesized `skills.jsonl` carrying four `stall` + four matching 30,000-frame `error` records with byte-identical samples (no `start`/`success`/`deferred`/`skipped` records). `logs/progress.jsonl` and `STATUS.md` absent, `logs/skills.jsonl` absent (pre-run harness). The dumps predate commit bf6b501, so `_dump_stall`'s `controller_stack`/`held_buttons`/`nav_intent` fields are absent. State: VERMILION_CITY (3,5) coords (20,24) facing Left, avatar flags = 8 = `AvatarFlags.Surfing` only, running_state MOVING / tile_transition NOT_MOVING (bump-in-place), no active script; all 8 badges, GOT_HM05 false; party Blastoise L100 (Strength/Rock Smash/**Surf**/Dig) / Jynx L20 / Zapdos L82 / Mewtwo L92 / Porygon L26 / Koffing L28 all full HP; bag 10 Poké + 4 Ultra Balls, Old/Good/Super Rod, Rainbow Pass, S.S. Ticket; dex_owned 122 / seen 161. **This OVERWORLD nav freeze IS fully reconstructible from the .ss1 and was reproduced deterministically** — unlike the DRYRUN-001/002/003 nav freezes — because the blocker lives in game RAM (a live map object) and the path that walks into it is a pure function of that RAM.

Seats: A=claude-sonnet-5  B=claude-opus-5[1m]  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. Spawned via sub-agent alias `sonnet` (the spawn interface accepts only aliases, not full §3 IDs); self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). Spawned via alias `opus`; self-reported `claude-opus-5` (harness variant `claude-opus-5[1m]`, the 1M-context variant). **MATCH — the harness limitation recorded as a VIOLATION in DRYRUN-001…006 (only Opus 4.8 available) no longer applies: claude-opus-5 exists in this harness and occupied the seat.** Seat B declined Fable escalation (§3 ruling in exchange 2).
Model check C: required §3 = Codex GPT-5-class, different provider than Seat B. Codex over MCP (account-default model; explicit `gpt-5.x` override not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Self-reported "Codex, GPT-5-class"; it stated the exact backend id is not exposed to it and declined to overclaim. Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event, opened at event open per §5 and continued with the continuation tool for every subsequent turn: threadId **019fbd8a-d1ca-7530-9bf5-704e90398f67**.

Exchanges: 4/6

Exchange 1 — Seat A (Diagnostician), position: **CODE DEFECT, upstream layer (pokebot-gen3), with a dexbot-layer consequence.** NOT a KB error (geometry/flags/party/encounter data all verified correct against `data/encounters.json:15541-15555` and the live save), NOT a planner deadlock, NOT a transient (deterministic given the observed state), NOT already-known (FINDINGS index empty → no rediscovery). Mechanism pinned to a line-level check-order bug in `pokebot-gen3/modules/map_path.py`'s `is_tile_accessible` (inner fn of `calculate_path`, `def` at :914), evaluated in this order: the `dynamic_collision_flag` gate (:933-938) blocks only when the live object is *not* currently loaded — not this case, object local_id 7 is `active` — then the **unconditional** surf-dismount return at **:947** (`if tile.elevation == 3 and from_elevation == 1: return True`) fires for the water(20,24)→sand(19,24) step and returns "accessible" **before** the live-object obstruction check at **:951** (`if tile.global_coordinates in blocked_coordinates: return False`) is ever reached; `blocked_coordinates` is built at :893-907 from `get_map_objects()` and does contain (19,24). "So the pathfinder is not generically 'surf-blind' — it has dedicated surf/land-transition logic — it's that this transition logic short-circuits ahead of live-object collision for exactly this transition class." Severity **HIGH**: a fully deterministic, silent, self-unresolvable freeze burning the entire 30,000-frame budget. Blast radius: **observed** narrow (this tile; four dumps; plus DRYRUN-002 at the same map/tile/facing on a *different savestate and epoch*, which cross-validates it as a stable structural defect rather than a fixture artifact); **inferred wide-latent** from the driver's 39-static + 5-moving blast-radius scan, which Seat A explicitly did NOT re-run and flags as driver-sourced. On the recurrence: four byte-identical samples with exactly 32,000-frame gaps and zero intervening frames rule out an NPC-transient or RNG explanation; dexbot's blacklist/interruption machinery (`dexbot/navigation.py:909-1009`, reachable only via `except SkillError`/`except BotModeError` at :971/:983) **never engages**, because `follow_waypoints` has a genuine bounded 20-frame per-step timeout (`walking.py:158-159`, OVERWORLD-only decrement :193-194, raise :196-200) that `navigate_to`'s `while True: … except TimedOutTryingToReachWaypointError: pass` (:377-405) swallows, and `calculate_path` — a pure function of world state the failed step cannot change — re-emits the identical broken "Left" waypoint forever; only the global watchdog converts it to a `SkillError`. **Ruling on DRYRUN-002: SUPPORTED IN OUTCOME, REFINED IN MECHANISM** — location, layer and framing were right and are now cross-validated, but (a) the mechanism is a check-order bug, not generic surf-blindness, and (b) the walker pathology is an unbounded *retry* of a bounded per-step walker, not an unbounded *hold* — "which matters for where a guard would need to sit." Seat A additionally verified by direct parse that epoch `4+snx12` carries the (19,24) cut edges including stand tile (20,24) while epochs ≥5 badges carry zero Vermilion edges, and flagged the planner's asymmetric retry coverage (`planner.py:584` caps per-species at 2 and defers at :603-619, but `restock_pokeballs_if_low` :538-544 and `assemble_party` :560-568 are single-try with no defer counter and `grind_levels` :576-581 has no try/except). Five numbered open questions to Seat B. Confidence: high on mechanism (code read line-by-line); explicit about inference vs observation throughout. Sources: `map_path.py:893-907/914/933-944/945/947/951/416-425`; `walking.py:158-159/193-200/377-405`; `navigation.py:909-1009`; `runner.py:39/407`; `planner.py:531-535/584/603-619`; `data/nav_graph.json` (own parse); `data/encounters.json:15541-15555`; driver memory-state observations.

Exchange 2 — Seat B (Fix Designer), position: **agrees with Seat A's mechanism, re-verified line-by-line, and sharpens it three ways.** (1) *Ordering bug, not missing feature*: :933-944 exists precisely to say "if the object is loaded, don't trust its template — trust its live position", and the live position is consulted **only** at :951, so :945/:947 skip the very branch the preceding branch delegated to (`map_path.py:416-425` sets `dynamic_collision_flag`/`dynamic_object_id` from the template, confirming the split) — which also makes Seat C's event-open nuance the correct scope statement: the leaking class is **active/loaded objects only**. (2) *The engine's own predicate contains the missing conjunct* — from pret/pokefirered `src/field_player_avatar.c` (§10-allowed, fetched this session): `CanStopSurfing()` = `(gPlayerAvatar.flags & PLAYER_AVATAR_FLAG_SURFING) && MapGridGetElevationAt(x,y) == 3 && GetObjectEventIdByPosition(x,y,3) == OBJECT_EVENTS_COUNT`, reached via `if (collision == COLLISION_ELEVATION_MISMATCH && CanStopSurfing(...)) return COLLISION_STOP_SURFING;` — `map_path.py:947` models conjuncts 1-2 and **drops conjunct 3**, stating the defect as a one-line spec delta. (3) The retry is not merely unbounded but **provably non-convergent** (a pure function of state the failed step cannot change), so a guard counting retries is right in principle and one counting frames is a proxy. Seat B explicitly **refused to sign** the 39+5 scan as its own verification (twice-attested, Seat-B-unverified) and noted its fix ranking depends only on "≥1 other instance exists", which DRYRUN-002's same-tile recurrence already establishes. **Layer ruling from inspecting `patches/`:** `pokebot-gen3/` is gitignored (`.gitignore:8`) and fully untracked (`git ls-files pokebot-gen3` = 0 files); `setup.sh:7,14,17` clones, pins `POKEBOT_COMMIT=5dd898f8`, then `git -C pokebot-gen3 apply ../patches/*.patch || true`; `patches/0001-upstream-fixes.patch` (499 lines) is byte-identical to `git -C pokebot-gen3 diff` bar abbreviated-hash width in its 7 `index` lines. Therefore `patches/` is not merely the right home but **the only durable one** (a bare edit to the vendored file is invisible to git and evaporates on re-clone, and `apply --check … || true` silently no-ops on an already-patched tree, so the two artifacts can drift with zero signal); both must change in one commit whose only tracked diff is the patch file; and **§9.3 precedent already exists inside this exact function** — the `_flag_door_passable` early return at :915-919 is dexbot's own patch, its comment reading "*Static collision/accessibility is meaningless for switch doors; an open door is plain floor (but still respect live objects)*", implemented as `return door and tile.global_coordinates not in blocked_coordinates` — so the fix is the consistent application of a rule this council already wrote into `patches/`, not new policy on shared code. **FIX 1 (necessary AND sufficient; upstream via `patches/`; written only):** in `is_tile_accessible`, delete the `blocked_coordinates` guard at :951-952 and reinstate it immediately before the two elevation-transition returns at :945/:947 (net two lines moved up, plus a comment citing `CanStopSurfing` and this event id), then regenerate the patch with `git -C pokebot-gen3 diff > patches/0001-upstream-fixes.patch`. Chosen in exactly this shape because the driver's in-memory experiment executed *this* transformation and observed the correct re-route, the correct `PathFindingError`, and a hand-followed physical arrival. What it does NOT fix, stated: the retry-storm class (→ Fix 2); `catching.py::_shore_tiles` still nominating occupied tiles (→ Fix 3); objects unloaded at plan time (correctly left to the template path). Named behaviour change: a *moving* NPC transiently on the sole shore tile now yields `PathFindingError` → `walking.py:361-362` → `BotModeError("Could not find a path…")` instead of a silent timeout loop — already upstream's semantics for every non-transition step, and **dexbot already owns the receiving handler** (`navigation.py:1000-1013`: match, wait 120 frames, retry, blacklist after 3 strikes) — "Fix 1 converts an unhandled infinite freeze into an already-instrumented, already-bounded failure path." Deliberately accepted asymmetry, flagged as unverified inference: the hoist also covers the **mount** direction (:945), and Seat B could not settle from the decomp (its `GetCollisionAtCoords` fetch exceeded the window twice) whether the engine permits mounting Surf onto an object-occupied water tile, so the patched pathfinder may be stricter than the engine there; **narrower fallback offered**: leave :945 in place and insert the guard between :945 and :947 (dismount-only), which still fixes this event in full. **FIX 2 (companion, neither necessary nor sufficient; dexbot-local):** `NavLegStalled(SkillError)` + `_walk_leg_guarded(map_key, coordinates, run=True, *, budget_frames=1800)` in `dexbot/navigation.py`, wrapping upstream's single-level walker and counting only frames on which the avatar *could* have moved (OVERWORLD **and** controllable **and** no active script, mirroring `walking.py:193-194`), raising after `budget_frames` of zero net displacement; applied at the three sites this module owns (:185 `perform_cut`, :950 final leg, :968 warp leg) plus a new `except NavLegStalled` arm placed **before** the existing `except SkillError` at :971 that blacklists `current_target` immediately instead of consuming 30 generic replans. Budget rationale (numerically correcting DRYRUN-002): 480 frames sits *inside* `walking.py:180-186`'s own legitimate allowances (+280 warp, +300/+400 surf-mount) and would false-positive; 1800 (~30 s controllable overworld) clears them and stays well under the 30k watchdog so it always fires first. Explicitly bounded out: the other ~49 direct upstream-walker call sites across `dexbot/{story,gyms,openings,safari,boulders}.py` (52 uses total, 3 in `navigation.py`) — the same gap DRYRUN-002 documented, consciously deferred to a per-module mechanical follow-up. **FIX 3 (optional hygiene):** skip live-object-occupied tiles in `dexbot/catching.py::_shore_tiles` (:133-165, whose `is_dry_land()` at :159-165 tests only `not tile.is_surfable and not tile.collision`) — cosmetic once Fix 1 lands. **Alternatives considered and REJECTED in writing:** (A) DRYRUN-002's `_walk_leg_guarded(480)` **alone** — quantified: the `SkillError` lands in the existing `except SkillError` at :971, which clears the route and continues; `_plan_warp_route` returns an empty route for a same-level target, so control returns to :950 and re-wedges → ~30 interruptions × 480 ≈ 14,400 frames, then raise: it halves the burn and names the error but **the objective still fails and the species is still never caught** → *supported as defence-in-depth, insufficient as the fix, numerically wrong as proposed*. (B) widen the `dynamic_collision_flag` branch to also block when the object IS loaded (Seat A's Q1 belt-and-suspenders) — rejected as actively harmful: it would block the object's *template* tile after the object walked off it, a false wall on every wanderer's spawn tile on every map, and still wouldn't block the live tile. (C) a static `blockers` entry for Vermilion (19,24) (the mechanism the patch already uses for ROCKET_HIDEOUT_B1F) — rejected: fixes 1 of 39+5 sites; is wrong after the tree is cut unless keyed to flag 18, and the tree **respawns on map reload** (documented at `dexbot/gyms.py:106-108`), so a flag-keyed static blocker lies in both directions; and it would break `perform_cut`'s own approach to that tile — the same failure the patch's own comment records for Victory Road boulders ("no static VR Strength-boulder blockers here. They break `run_boulder_puzzle`"). (D) detect "first waypoint refused" inside `follow_waypoints` and re-plan with an exclusion set — rejected: needs a new upstream API threaded through `calculate_path`'s signature plus generator plumbing (far larger upstream surface than a two-line reorder), duplicates `blocked_coordinates`, and would not help `_walkable`/`build_navgraph`, which call `calculate_path` with no walker. (E) **Cut the tree instead of routing around it — rejected on evidentiary grounds**: the epoch-`4+snx12` graph literally encodes cutting it while standing on **water**, Seat B could not establish that FRLG permits the Cut field move from a Surf state, and the driver's A-press test exercised only the overworld interaction, not the party-menu field move — "proposing a fix that depends on an unverified game fact is precisely the §5 defect Seat C must FAIL"; it notes the correct land-side approach already exists at `dexbot/gyms.py:120` → `_cut_tree(VERMILION_CITY, (19,24), (19,25), "Up")`. (F) blanket-hoist the guard to the top of `is_tile_accessible` — rejected narrowly: most auditable and would cover the waterfall returns too, but widens the behavioural delta beyond the diagnosis (the ledge/`Jump `/waterfall classes this patch itself introduced) for zero benefit here, violating §9.3's "the change matches the diagnosis"; kept on the shelf for a future waterfall/forced-movement instance. (G) lower the 30k OVERWORLD watchdog — rejected (`runner.py:43-44`'s own rationale; trades a silent-stall class for a false-positive class; objective still unachievable). (H) PARK — rejected (§8.2 is for double-cap events; a two-line fix with executed evidence exists, and parking would forfeit Vermilion's five rod species plus whatever else the scan gates). **Answers to Seat A's five questions:** Q1 reorder only, do not touch :933-944 (per rejected-alternative B). Q2 disagrees that the caller is unrecoverable and names a suspect *by looking*: `catching.py::_shore_tiles` enumerates fishing spots from **static** metadata only, so (19,24) and the whole x=19 sand column are shore candidates; `catching.py:583` samples five, `:599-604` filters to graph-plannable ones, `:621-629` calls `dexbot.navigation.navigate_to` per candidate → with no Vermilion cut edges at this epoch `_plan_warp_route` returns empty and control reaches the **wrapped** `navigation.py:950`; corroborated by the three rods in the bag and `fishing_encounter_rate: 10` with five rod species at `"3,5"` — labelled **inference, high plausibility, not proof**, with the consequences that DRYRUN-002's raw-call coverage gap is NOT implicated (good for Fix 2's three-site scope) and that the blast-radius claim does not rest on it. Q3 rejects all three of Seat A's options: `planner.py:584`'s `range(2)` is not the driver because with the advisor at its deterministic default `consult_on_failure` returns `"defer"` and :615-620 breaks on attempt 0 (a catch objective normally stalls **once**); the likelier story is four *distinct* Vermilion rod objectives each deferring after one stall (five rod species at `"3,5"`; a deferred objective spends nothing, which is why money and party HP are byte-identical with zero intervening frames; plus the `deferred.clear()` cycle at `planner.py:531-535`) — but the **count itself is partly a fixture property**: a real run also emits `start`/`success`/`deferred`/`skipped` (`runner.py:492,496`; `planner.py:619,544,233`) and the evidence holds only the `stall`+`error` pair `_dump_stall`/`except BaseException` produce, so the count must not be read as a recurrence signature. It adds a *genuine* uncapped-repeat gap: the sample decodes to 10 Poké + 0 Great + 4 Ultra = **14 balls** against `restock_pokeballs_if_low(minimum=15)` summing exactly those three (`planner.py:222-225`) → `needed = 1`, so restock fires at **every** objective boundary in this save, and with ₽136 it drops into `_fund_by_selling` → `_earn_by_vs_seeker` → `_earn_by_patrol` (:235-240) — all walking skills — from a single-try call site with no defer counter: real hardening gap, wrong event. Q4 both, upstream first, `patches/` the only durable home; and rejects Seat A's "identical first waypoint across N replans" as **unobservable from dexbot** (the replans happen inside `walking.py:377-405`, entered via one `yield from`), so the observable proxy is zero net displacement — what Fix 2 implements. Q5 **split verdict**: the ≥5-badge absence of Vermilion cut edges is **correct** (at `BADGE05_GET` both yard sides are mutually reachable by surfing, so `build_navgraph.py:194-197`'s "different components" test rightly mints none), but the epoch-`4+snx12` **water stand tile is a real latent defect** with two named causes — `build_navgraph.py:178` accepts any adjacent `collision == 0` side without checking elevation or surfability, and `map_path.py:947` carries **no `can_surf()` guard** (unlike its mount twin at :945, which requires `BADGE05_GET` + a Surf user via :872) — so a water tile survives component assignment at 4 badges where Surf is unusable, and the graph then offers `perform_cut(…, stand=(20,24))` which `perform_cut` walks to at `navigation.py:185`; out of scope here, should be recorded as a finding, and an implementer must be told not to widen the diff to it. §9 assertions it would demand were this live: (1) the path to (19,20) no longer starts at (19,24) and begins `Up (20,23), Up (20,22)`; (2) the (19,24) request raises `PathFindingError`; (3) upstream `navigate_to((3,5),(19,20))` terminates ≤4,000 frames at (19,20) with flags == `OnFoot`; (4) same for `dexbot.navigation.navigate_to`; (5) **negative control** — the path to (20,22) (unoccupied Sand) still succeeds and physically dismounts, "without this, Fix 1 could pass 1-4 by simply disabling surf dismounts"; (6) all three prior dumps satisfy 1-5 too; (7) if Fix 2 ships, prove it against an **unpatched** `map_path.py` (it can never fire once Fix 1 is in) plus a false-positive control leg containing a surf mount. Regression: `BASELINE.md` 74 passed/2 failed (the Psyduck pre-Brock KB drift), no new failures, `tests/test_m7_surge.py` singled out as the most valuable pre-existing guard because `_escape_vermilion_gym_yard` → `_cut_tree((19,24),(19,25),"Up")` exercises **the same tree from the land side**; plus a NEW committed test it designed but did NOT run (`tests/test_map_path_object_blocking.py`: monkeypatch the module-scope `modules.map_path.get_map_objects` to return one fake non-player object at (19,24) over an existing 8-badge fixture, assert the (19,24) path raises, the (19,20) path avoids it, and moving the fake object restores the (19,24) path). `pokebot-gen3/tests/` ruled out of scope per `BASELINE.md` **and** shown to be no protection (`test_pathfinding.py` covers only indoor Pallet/Littleroot walks and warps — no water, no elevation transition, no live-object obstruction). Diff must NOT contain a `data/nav_graph.json` rebuild (Fix 1 changes `_walkable`'s answers → component-id churn across all 11 epochs) or the Q5b `can_surf()`/`build_navgraph.py:178` changes. Residual risk, ranked: mount-side strictness (unverified inference); `PathFindingError` where a transient timeout used to occur (mitigated three ways but named); Fix 2's 1800 is reasoned not measured ("if the false-positive control trips, raise it — do not delete the guard"); the new test is designed not run; A* tie-breaking could shift an exact-waypoint assertion on an NPC-adjacent route (none found in `tests/`, not all 31 files audited). **Escalation ruling (§3): NOT WARRANTED** — Seat A classified the event cleanly, Seat C has returned zero FAILs, and the fix is a two-line reorder inside an existing function plus an optional wrapper in an existing module, "the opposite of a new subsystem."

Exchange 3 — Seat C (Quality Gate, Codex GPT-5-class), verdict: **PASS-WITH-CONCERNS** on the written proposal. Independently verified the load-bearing facts against the repo at event open and again at judging (it also reproduced the pathfinder behaviour itself with a read-only harness over the vendored ROM data, getting the same 1-waypoint `Left` path, and independently reproduced the driver's 39-static/5-moving blast-radius counts). Axis findings — **Classification: PASS** ("the decisive repo fact": live object tiles are collected into `blocked_coordinates` at :891 but the surf/land early returns at :945 run before the live-object check at :951; "Seat A and Seat B correctly refine DRYRUN-002: the defect is not generic 'surf blindness'; it is active/live-object collision skipped on elevation-transition steps"). **Citations: PASS-WITH-CONCERNS** — core citations verify (`blocked_coordinates`, the dynamic-object/template split, the surf-transition ordering, the upstream walker's swallowed retry, dexbot's final-leg call site, patch durability through `patches/`, empty `FINDINGS.md`); concerns are precision, not invalidation: the `stall_g*.ss1` artifacts live outside the checkout so savestate-derived facts must be logged as **driver-provided memory-state observations**, and **REFUTATION (specific, §5)**: Seat B's "`:599-604` prefers graph-plannable candidates *but falls back to the unfiltered list*" is wrong for the rod path — `catching.py:599-603` **raises `SkillError`** when `rod_name is not None and not feasible`, so there is no fallback on zero feasible rod candidates; `candidates = feasible or candidates` only matters after that guard. **Coherence: PASS-WITH-CONCERNS** — Fix 1 is diagnosis-coherent and sufficient, converting the bogus route into either a route around the tree or a `PathFindingError` dexbot already handles at `navigation.py:1000`; the concern is exact patch shape, and Seat C **closed Seat B's stated residual risk from the decomp**: FireRed's dismount explicitly checks object occupancy via `GetObjectEventIdByPosition` in `CanStopSurfing()` (pret `src/field_player_avatar.c`), whereas Surf *setup* goes through `IsPlayerFacingSurfableFishableWater()` (pret `src/party_menu.c`) → `GetCollisionAtCoords`, which returns **elevation mismatch before object collision** (pret `src/event_object_movement.c`) — so the decomp supports the missing conjunct for the **dismount** but does **not** establish engine parity for a symmetric mount-side guard. **Alternatives: PASS** ("unusually complete"); rejecting guard-only as insufficient, the template-branch widening, the Vermilion-only static blocker, "Cut the tree" as unverified from a Surf state, and the nav-graph churn all check out, as does the `patches/` durability reasoning. Explicit rulings: **(1)** Fix 1 alone is sound and complete for this event, and the **dismount-only guard placed before `map_path.py:947` is the preferred shape** — the symmetric mount+dismount hoist is broader than the event diagnosis unless one deliberately prefers conservative bot semantics over engine parity. **(2)** Seat B's ruling on DRYRUN-002's `_walk_leg_guarded` is correct (defence-in-depth, insufficient alone, 480 too low), but its arithmetic is off: `navigation.py:971` increments `interruptions` and :977 raises only when `> 30`, so it is **31** expiries ≈ **14,880** frames at 480, not ≈14,400 — conclusion unaffected. **(3)** Seat B's `_shore_tiles` caller identification checks out as an inference but "high plausibility" needs qualification: Seat C's own static reproduction of the sampling did **not** place (19,24) among the first five sampled shore candidates, so the right status is "plausible caller path, not proven telemetry." No forbidden scope fetched.

Exchange 4 — driver → Seat C (same thread, new evidence, not a restatement): I executed the actual sampling against the stall savestate. `_shore_tiles(VERMILION_CITY)` returns **136** candidates and **does** include `(19,24)` facing `Right` (x=19 column: (19,23)/(19,24)/(19,25)/(19,26)/(19,27) Right, (19,28) Down, (19,11) Down). **Seat C's narrow point is correct**: `shore[::max(1,len(shore)//5)][:5]` yields `[(24,23), (24,31), (32,28), (19,11), (8,28)]` — the tree tile is not a sampled destination; and I confirm its §5 refutation of the "unfiltered fallback" claim (`catching.py:600-603` raises for the rod path). **But every one of those five sampled candidates is reached by a path whose FIRST waypoint is `Left (19,24)`** from the stall position ((24,23) 9 wps, (24,31) 45, (32,28) 34, (19,11) 20, (8,28) 16), and all five pass `_plan_via_graph`/`_walkable`, so `feasible` is all five and the rod guard never fires — the wedge does not require the tree tile to be a *destination*, it is the shared first *step*, and `catching.py:621-629`'s per-candidate `except (BotModeError, SkillError): continue` (the mechanism that would try candidate 2) is defeated because the wedge never raises. Seat C's final ruling on the record: log ruling #3 as **"strengthened, with one citation correction preserved"** — its narrow correction stands and Seat B's fallback claim remains wrong for rod objectives, while the caller hypothesis is recorded as **high-confidence inference materially strengthened by driver replay; plausible caller path, not proven telemetry**; **verdict unchanged (PASS-WITH-CONCERNS)**; **ruling #1 unchanged** (dismount-only guard before :947 preferred — the new evidence still exercises only water→land dismount and adds no support for symmetric mount-side hoisting); and the 31-expiry / 14,880-frame arithmetic correction stands as a recorded concern.

Additional driver verifications supporting the above (read-only, sandboxed): `dexbot.navigation._walkable(((3,5),(20,24)), ((3,5),W))` returns **True for all ten** Vermilion warp nodes W and `_plan_warp_route` from the stall tile returns **`[]`** for in-town destinations — so dexbot's own planner believes the pool is walk-connected *via the same buggy check* and then hands the final walk to it at `navigation.py:950`; this is caller-independent mechanism-level support for Seat B's Q2 answer. `_walkable`'s own docstring (`navigation.py:108-114`) states dexbot **relies** on the assumption this bug breaks: "*`calculate_path` blocks the live NPCs' tiles, so a wandering NPC in a choke point makes checks fail transiently*". The `components` map in `data/nav_graph.json` is keyed on **warp/portal tiles only** (10 Vermilion entries, all one component at 8 badges), so "the graph does not model the pool" is a property of the data structure rather than a missing route — which resolves the premise of Seat A's Q5 concern in the direction Seat B argued. `get_map_objects()` (`pokebot-gen3/modules/map.py:1799-1808`) returns only objects whose active bit is set, corroborating that the leaked class is active/loaded objects. The four `.ss1` files differ from one another by ~3,600 bytes of 397,312 — distinct emulator states 32,000 frames apart, not copies; this is consistent with both four genuine back-to-back retries and harness generation, so per Seat B's Q3 the count is not read as a recurrence signature.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9's replay/regression gate inapplicable; nothing implemented). The PASS attaches to: (i) the classification (CODE DEFECT, upstream `pokebot-gen3`, HIGH severity — live/active-object collision skipped on water↔land elevation-transition steps in `is_tile_accessible`); (ii) **FIX 1 as necessary and sufficient**, in the **dismount-only shape** Seat C ruled for — the `blocked_coordinates` guard inserted before `map_path.py:947` (leaving the mount-side return at :945 as-is, since the decomp supports the missing conjunct only for the dismount), shipped through `patches/0001-upstream-fixes.patch` as the only durable home; (iii) Seat B's no-Fable-escalation ruling; and (iv) the rejection of "Cut the tree" on the §5 evidentiary ground that Cut-from-Surf is unverified. **NOT** covered by this PASS: FIX 2 (`_walk_leg_guarded`/`NavLegStalled`) and FIX 3, which are behavioural and would each require their own §9 replay/regression evidence before shipping — FIX 2 specifically must be proven against an *unpatched* `map_path.py`, since with Fix 1 in place it can never fire.
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction (A classified and pinned the mechanism to the line; B confirmed it, supplied the engine-side specification, and pre-authorised the dismount-only shape as its own stated fallback). No verdict disagreement to resolve under §7. Dissent: none. Concerns (recorded intact, never deleted): (i) Seat C's §5 refutation — Seat B's "`:599-604` … falls back to the unfiltered list" is **wrong for rod objectives** (`catching.py:600-603` raises `SkillError` when no rod candidate is feasible); driver-confirmed; (ii) the exact patch shape — the symmetric mount+dismount hoist Seat B ranked first is **broader than the diagnosis**, because Surf setup routes through `GetCollisionAtCoords`, which returns elevation mismatch before object collision (pret `src/event_object_movement.c`), so the dismount-only guard is what ships; Seat B's mount-side residual risk is thereby closed rather than accepted; (iii) Seat B's ≈14,400-frame arithmetic for DRYRUN-002's rejected guard is off — `interruptions > 30` means **31** expiries ≈ **14,880** frames; conclusion unaffected; (iv) the Q2 caller identification (`catching.py::_shore_tiles` → `navigation.py:950` on a Vermilion rod objective) is **high-confidence inference materially strengthened by driver replay, not proven telemetry** — the objective label is scrubbed, `skills.jsonl` is synthesized, and (19,24) is not among the five sampled destinations (it is the shared first *waypoint* of all five); (v) all savestate-derived facts are **driver-provided memory-state observations** — the artifacts live outside the checkout and Seat C could not read them; (vi) the four-dump count must not be read as a recurrence signature (only the `stall`+`error` pair is present, and a real run would also emit `start`/`success`/`deferred`/`skipped`) — the *mechanism*, not the count, is what four dumps corroborate; (vii) deliberately left unfixed and recorded: the ~49 unguarded direct upstream-walker call sites (the DRYRUN-002 gap), the single-try/unwrapped `restock_pokeballs_if_low`/`assemble_party`/`grind_levels` call sites (**guaranteed live in this save**: 14 balls against `minimum=15`, and ₽136 drives the funding fallbacks at every objective boundary), and the epoch-`4+snx12` water **stand tile** for the Vermilion cut edge (`build_navgraph.py:178` accepting any `collision == 0` side + `map_path.py:947` lacking a `can_surf()` guard) — each a separate event, and an implementer must be told not to widen this diff to them.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written. No commit, no PARKED routing). Were this live: FIX 1 (dismount-only guard before `map_path.py:947`, plus the regenerated `patches/0001-upstream-fixes.patch` in the same commit referencing this event id, plus the designed `tests/test_map_path_object_blocking.py`) would ship as the event's fix commit and the bot resume; FIX 2 and FIX 3 would open as separate replay-gated changes. **NOT a PARKED objective** — the route exists in-game, the fix is two lines, and the affected objectives (at minimum Vermilion's five rod species) are recoverable.
Finding: none (dry-run must not write FINDINGS.md; were this a live event, TWO findings would record: (1) a deterministic silent OVERWORLD freeze while surfing at VERMILION_CITY (20,24) facing Left, because `calculate_path`'s `is_tile_accessible` returns "accessible" for a water→land dismount (`map_path.py:947`) **before** consulting live-object collision (:951), routing the walker onto (19,24) where an uncut `EventScript_CutTree` object stands (template flag 18, unset; `inanimate`, never moves) — the engine's own `CanStopSurfing()` requires `GetObjectEventIdByPosition(x,y,3) == OBJECT_EVENTS_COUNT`, the conjunct the pathfinder drops — so the 20-frame per-step timeout fires, upstream's `while True` swallows it and replans an identical path from unchanged state, and the leg burns the full 30,000-frame budget; the same blind spot makes `dexbot.navigation._walkable` report the pool as walk-connected, so dexbot's own re-plan/blacklist machinery never engages; fixed by hoisting the `blocked_coordinates` guard above the dismount return, via `patches/`; blast radius measured at 39 static + 5 moving FRLG objects standing on water-adjacent land. (2) the epoch-`4+snx12` Vermilion cut edge nominating a **water** stand tile (20,24) — `build_navgraph.py:178` accepts any adjacent `collision == 0` tile and `map_path.py:947` has no `can_surf()` guard, so a water tile survives component assignment at 4 badges where Surf is unusable — with the reopen condition that an event shows `perform_cut` actually routed to it.)

Driver disclosures (side effects, all restored):
- Ran six headless read-only reproductions on the four `stall_g` savestates via the persistent-profile emulator (`dexbot.emulator.setup_headless_emulator`, `DEXBOT_VIDEO=0`, `is_test_run=True`): (1) full RAM decode of all four dumps (state/coords/facing/avatar flags/tasks/script context/party/bag/flags/dex); (2) Vermilion tile grid, live map objects, ROM object templates, 3,000 idle frames, held-direction tests (Left/Up/Down/Right ×300f), and `calculate_path` probes to twelve destinations; (3) wedge reproductions — upstream `walking.navigate_to` to (19,20) and (14,26) (3,000 frames each, zero movement) and dexbot `navigation.navigate_to` to (19,20) (4,000 frames, zero movement) — plus the **check-order experiment**, in which `calculate_path`'s source was read with `inspect.getsource`, the `blocked_coordinates` check moved above the two elevation-transition returns, and the patched copy `exec`'d **in memory only** (no file on disk was modified), then the re-routed path hand-followed to (19,20); (4) blocker identity (event flag 18, object graphics/movement/flags, A-press-while-surfing test, `gStringVar4` read) and the all-FRLG-maps blast-radius scan; (5) `dexbot.navigation._walkable`/`_plan_warp_route` probes from the stall tile; (6) `dexbot.catching._shore_tiles` sampling + `_plan_via_graph` feasibility + first-waypoint check for the five sampled candidates. All frames were advanced by hand via `context.emulator.run_single_frame()` (never `run_skill`); `context.emulator.create_save_state` was stubbed to a no-op, `runner._events_path` redirected to a scratch dir outside the repo, and `runner._checkpoint_phase`/`runner._dump_stall` monkeypatched to no-ops. No telemetry (`logs/skills.jsonl` still absent, `logs/` still empty) and no fixture writes (`fixtures/_stalls` still does not exist). (Cf. memory `stall-ss1-omits-controller-state`: this OVERWORLD **nav** freeze — unlike the DRYRUN-001/002/003 nav freezes and like the DRYRUN-004/005/006 menu/battle/message wedges — **IS** reconstructible from the `.ss1`, because the blocker is a live map object in game RAM and the doomed path is a pure function of that RAM; the memory note should be read as "nav freezes are unreconstructible *when the wedge is in Python controller state*", not as a property of nav freezes generally.)
- `pokebot-gen3/profiles/livingdex/` **already existed** as found (`metadata.yml` md5 8809e386fbe047116a9ad0dfe102e931, `stats.db` md5 e446606a5d81ea6dc06481a5e3b23e61) — unlike DRYRUN-001…006, no profile dir had to be created or removed. Both files are byte-identical after all runs and **no `current_state.ss1` was written**; `pokebot-gen3/profiles/` is exactly as found (10 loose config files + `__pycache__` + `livingdex`). (Cf. memory `repro-writes-profile-dir`.)
- The four evidence savestates are byte-intact (md5 1e97e392be56337764203a3b9abca9b9 / 42debf71ad9346413a72839a357bb725 / fd0bc9c4ae02ff2c6e5e985aa9861247 / 9a7c58040c32e30f0fd03af3314e3cf6, identical before and after every run).
- Repro scripts were written to a scratch directory **outside** the repository (`%TEMP%\dryrun007_scratch`), never into the checkout.
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, `data/`, `patches/`, or any `dexbot/` or `pokebot-gen3/` source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md; `git -C pokebot-gen3 status` shows exactly the 7 files the pre-existing `patches/0001-upstream-fixes.patch` modifies, unchanged from as-found.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`; the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-008 — 2026-08-01T16:05:55Z
Trigger: §4.1 watchdog_stall — the runner progress watchdog (`dexbot/runner.py:39` `_PROGRESS_BUDGET_FRAMES`=30_000, OVERWORLD budget selected at :415-425 because game_state=OVERWORLD; sampled every 2,000 frames at :407; dump+raise at :458-463) dumped a savestate for objective 'stall_h' (dry-run: historical, scrubbed name) with the progress sample `((3, 35), (11, 18), 4588, 1, 26, 7, (46, 30, 142, 77, 40, 54))` = map (3,35)=`MapFRLG.ROUTE17` the **Cycling Road** (`map_data.py:306`) / coords (11,18) / money ₽4588 / 1 Poké Ball / 26 Great Balls / game_state 7 OVERWORLD / party-HP. Evidence: `stall_h.ss1` (md5 fd8e0c8c9bb26737dc998636cbe51f65) + `stall_h_prior1.ss1` (md5 703c72ec1095329830a31038d0581f84) — **two dumps of the same wedge**, distinct emulator states (a wandering NPC sits at different coords in each) — plus `stall_h_state.json` and a harness-**synthesized** `skills.jsonl` carrying exactly two `stall` records (byte-identical samples, `"script": []`) and two matching 30,000-frame `error` records; no `start`/`success`/`deferred`/`skipped` records, so per DRYRUN-007 concern (vi) the dump COUNT is NOT read as a recurrence signature and the absence of pre-stall telemetry is treated as unknown, not as absence of events. `logs/skills.jsonl`, `logs/progress.jsonl` and `STATUS.md` absent (pre-run harness). The dumps predate commit bf6b501, so `_dump_stall`'s `controller_stack`/`held_buttons`/`nav_intent` fields are absent from the artifact (the driver reconstructed them by replay — see R10). State: ROUTE17 (11,18) facing **Down**, **on the Bicycle** (`avatar.flags==2`, Bicycle in key items); party Blastoise L55 46/161 (28.6%) / Paras L11 / Snorlax L31 / Gloom L28 / Cubone L15 / Fearow L25 asleep; badges 1-4 only; Cut yes, **no Fly, no Surf**; dex_owned 41 / seen 83. **This OVERWORLD nav freeze is fully reconstructible from the .ss1 and was reproduced byte-exactly** (R5) because the blocker is a live map object in game RAM and the wedged loop is a pure function of that RAM.

Seats: A=claude-sonnet-5  B=claude-opus-5[1m]  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. The spawn interface accepts only aliases, not the full §3 IDs, so spawned via alias `sonnet`; self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). Spawned via alias `opus`; self-reported `claude-opus-5` (harness variant `claude-opus-5[1m]`, the 1M-context variant). MATCH — as in DRYRUN-007, the harness limitation recorded as a VIOLATION in DRYRUN-001…006 no longer applies. Seat B declined Fable escalation (§3 ruling in exchange 2).
Model check C: required §3 = Codex GPT-5-class, a different provider than Seat B. Codex over MCP (account-default model; the explicit `gpt-5.x` override is not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event, opened at event open per §5 and continued with the continuation tool for every subsequent turn: threadId **019fbdd3-3810-7202-927b-63277ca68a08**.

Exchanges: 5/6

Exchange 1 — Seat A (Diagnostician), position: **CODE DEFECT** — a tile-model gap in `pokebot-gen3/modules/map_path.py` combined with a movement-completion blind spot in the upstream state machine, for a forced-movement tile class the pathfinder never models; **the demonstrated instance is ALREADY REMEDIATED in current mainline** by a targeted workaround, but the root cause is unfixed at source. NOT a KB error (no `data/` entry is implicated — `encounters.json`'s ROUTE17 table and the map identity are correct), NOT a planner deadlock (the failure lives inside a single skill's navigation primitive, below the planner), NOT a transient (R5 reproduces the byte-identical tuple deterministically), NOT already-known (FINDINGS/DECISIONS/PARKED all empty → no rediscovery possible). Mechanism, line-verified: `map_path.py`'s `PathMap.tiles` builder (`:111-359`) carries `elif` branches keyed on `tile.tile_type` for Jump (`:118-161`), Waterfall (`:167-191`), Muddy Slope (`:192-220`), rails (`:221-226`), Bumpy Slope (`:227-230`), the four Currents (`:231-285`), Spin (`:286-330`) and Ice (`:331-357`) — **none matches `"Cycling Road Pull Down"`/`"…Grass"`**, which fall to the generic catch-all at `:358-359` and never get `forced_movement_to` set; consequently `dexbot/navigation.py:824-842` `_has_spinners()`'s generic ≥4-direction detector **structurally cannot** see Route 17, which is why the mainline carries the hardcoded `_FORCED_SLOPE_MAPS = frozenset({(3,35)})` at `:821` with the comment block at `:813-820`. `AvatarFlags.ForcedMove` (bit 64, `player.py:25-33`) is unset at `avatar.flags==2`, so `player_avatar_is_controllable` (`player.py:263-284`) returns True and every upstream gate keyed on ForcedMove is blind; `player.py:36-47` decodes R3's idle histogram to `(MOVING, CENTERING)`/`(TURN_DIRECTION, NOT_MOVING)` — `running_state` is **never** `NOT_MOVING`, so four upstream stop-gates (`walking.py:58-60`, `:99`, `:303`, `:438-439`) can never fire. Severity: HIGH while live (a full unattended-run-ending stall caught only by the coarse 30k watchdog) but **not reproducible on current mainline** → blast radius for the demonstrated instance is retrospective. Residual claimed **MEDIUM/live**: `catch_species` calls `spin()` immediately after `navigate_to` with no check that the landing map is a forced-movement map, and `missing_catchable()` scores maps by encounter rate while ROUTE17 passes `_graph_reachable` — so an un-owned Route 17 land species would walk there fine (fixed) and hang forever in `spin()` (not fixed). Challenges to the driver pack: (a) R4 demonstrates collision avoidance, a *different* mechanism, and must not be read as "route execution is safe here"; (b) R5's `_spinner_maps.clear()` was probably redundant; (c) concurs R6 is a reproduction artifact. Four numbered open questions to Seat B. Seat A re-verified every cited line reference against the repo and was explicit throughout about observation vs inference.

Exchange 2 — Seat B (Fix Designer), position: **agrees with the classification, and corrects or sharpens Seat A on four counts.** (1) *Citation audit*: two cosmetic drifts (`player_avatar_is_controllable` is `player.py:263-284`; the "Route 17 biker fight" phrase is `runner.py:367`), plus one correction of substance Seat A missed — **`map_path.py` is already a dexbot-patched file**: `patches/0001-upstream-fixes.patch` carries 7 files including `map_path.py`, and the Spin branch Seat A cited as evidence of upstream's tile vocabulary **is dexbot's own patch** (hunk `@@ -249,6 +283,51 @@`), as is the Jump modelling at `:134-161`; `walking.py` is **not** in the patch at all. (2) *The real wedge site*: Seat A's four "can never fire" gates are all **downstream of the wedge and none is on the historical path**. The live path is `dexbot/navigation.py` → upstream `walking.navigate_to` (`:314-405`) → `follow_waypoints` (`:114-310`), and the wedge is at **`walking.py:172-175`** — for the FIRST waypoint only, `if waypoint.direction != current_facing_direction: yield from ensure_facing_direction(...)` — dropping into **`ensure_facing_direction` (`walking.py:445-479`)**, a `while True:` at `:467` with **no frame budget** whose press gate (`:472-477`) requires `tile_transition_state == NOT_MOVING and running_state == NOT_MOVING`. (3) *ROM-sourced upgrade of Seat A's inference to fact*: per pret/pokefirered, `MB_CYCLING_ROAD_PULL_DOWN = 0xD0` / `_GRASS = 0xD1` (`include/constants/metatile_behaviors.h`); `MetatileBehavior_IsForcedMovementTile()` (`src/metatile_behavior.c`) does **not** cover 0xD0/0xD1; `sForcedMovementFuncs[]` (`src/field_player_avatar.c`) has no cycling-road entry and `PLAYER_AVATAR_FLAG_FORCED` is set only inside `DoForcedMovement()` — so bit 64 is *provably* never set on Cycling Road; and `BikeInputHandler_Normal` (`src/bike.c`) writes `gPlayerAvatar.runningState = MOVING` on **every** frame the player stands on a pull-down metatile without B held, making the gate **unsatisfiable** and the loop press **zero buttons** forever. Bulbapedia (Kanto Route 17) states the mechanic in prose. (4) *R4 is the TRIGGER, not a red herring*: routing around the NPC makes the first waypoint East while the avatar faces South, so `walking.py:174` is True and only then does `:175` fire — without the NPC the first waypoint would be South, `:175` would be skipped, and the failure would have been the *recoverable* 20-frame timeout at `:196-200`. "The stationary NPC is not merely the physical obstruction; it is what routes execution into the unbounded turn loop." **Answers to Seat A's questions** — Q1: the 20-frame `TimedOutTryingToReachWaypointError` never fires because `:175` runs *before* the per-waypoint loop is entered (counter initialised at `:179`, decremented at `:193-194`, both downstream); sub-tile jitter ruled out (`map.py:1742-1750`, `player.py:320-322` read integral tile coords); "another controller" unsupported (`runner.py:386-387`, injections at `:381`/`:453` don't arm in a calm overworld). Q2 **rejected on three grounds**: a `map_path.py` tile-type teaching cannot touch `spin()`'s RAM gate (Seat A's own premise refutes it); the model would be *wrong* — every existing `forced_movement_to` encodes a deterministic landing, whereas the pull is re-evaluated per frame, suppressed by B, and overridden by held input in all four directions (R3: Up exits north *against* the pull), so there is no landing to encode and encoding one would break `walk_carefully`'s "waypoints are slide LANDINGS" premise (`navigation.py:743-744`) that makes R5 pass; and it is patch churn on a passing path. Q3 **rejected**: ROUTE17 is the strict argmax for **none** of its five land species (Spearow 30 vs 35 on `3,29`/`3,28`/`3,27`; **Doduo 35 ties `3,34` Route 16 and loses on `planner.py:144`'s strict `>` plus `dependencies.json` key order**; Raticate 25 vs 30; Rattata 5 vs 50 on `3,19`; Fearow 5 vs 25 on `3,42`), and every other `spin()` call site takes a fixed map — Seat A's residual downgraded MEDIUM → **LOW, guard by assertion not runtime filter**. Q4: record the coordinate coincidence as context only, never as evidence of label identity. **RULING: for the demonstrated stall, ship NOTHING** — R5 shows mainline completes it and `walk_carefully` is fully bounded (`max_repaths=8` `:738`, `settle` 900f `:754`, 40-frame tap cap `:791`, terminal SkillError `:808` into the re-plan handler `:971-982`); re-fixing a passing path is the gratuitous edit DRYRUN-004 forbids. **Proposed instead, ranked: FIX A** (dexbot-local pre-flight guard at `items_ground.py:50` skipping forced-slope maps, because `planner.py:469-474` calls `collect_item_balls(here)` with `here` = whatever map the player stands on, and `collect_item_balls` reaches the unbounded `ensure_facing_direction` at `items_ground.py:73`) — **flagged as INFERENCE with an explicit falsifier written into its own §9 replay: "if the PRE run does not stall, Fix A is unjustified and must be dropped"**; **FIX B** (bound `ensure_facing_direction`, `walking.py:467-479`: 120-frame grace then press anyway every 2 frames, `BotModeError` at a 300-frame cap — numerics anchored to upstream's own 20-frame step budget (`walking.py:155-158`), the 8-frame bike tile (`navigation.py:788-790`), `press_until`'s 600/interval-2 defaults (`runner.py:48`) and 1% of the 30k budget; framed as applying the rule already written at `runner.py:51-57`, made **conditional on driver probes R10/R11**, with the 96-call-site risk named as its weakest joint and a conservative raise-only variant offered); **FIX C** (test-only `tests/test_m3_forced_slope.py` asserting the ROM-scanned Cycling-Road map set **equals** `_FORCED_SLOPE_MAPS`, since R9 shows that constant rests on an undatable comment). **Layering ruling**: `pokebot-gen3/` is gitignored (`.gitignore:8`), `git ls-files pokebot-gen3` empty, `setup.sh` clones pinned `5dd898f8…` then applies `patches/*.patch` — so Fix B must add `modules/modes/util/walking.py` as the **8th** file in `patches/0001-upstream-fixes.patch`, regenerated in the same commit; a working-tree edit without patch regeneration is a §9.3 FAIL. A dexbot-local `face_direction` wrapper was weighed and **rejected**: it would not cover `walking.py:175` — where the historical wedge actually occurred — nor `:211`/`:221` nor `higher_level_actions.py:562` in `talk_to_npc`; 37 mechanical edits across 11 modules is a larger, riskier diff than one hunk; and it leaves two divergent turn helpers in the tree. **Ten alternatives rejected in writing**, including lowering the 30k watchdog (**rejected on numbers**: R5's single successful leg took 12,352 frames = only 2.43× the budget) and adding a press to `walk_carefully.settle()` (**rejected on mechanism**: settle is deliberately passive so a slide can resolve, and the natural button B *suppresses the downhill pull entirely* per `src/bike.c`, changing the landing physics the derail logic at `:801-803` is calibrated against). **Fable escalation (§3): NO** — Seat A classified cleanly, no Seat C FAIL, and all three items are patches to existing code. Seven residual risks named and ranked. Four driver probes requested (R10-R14).

Driver probes requested by Seat B, executed read-only and sandboxed between exchanges 2 and 3:
- **R10** — enriched `_dump_stall` fields captured at the PRE-GUARD reproduction: `held_buttons: []`, `controller_stack: ['_make_bot_mode.<locals>.DexSkillMode.run']`, `nav_intent: {'destination': ((3,7),(25,31)), 'leg': ((3,36),(41,9)), 'from': ((3,35),(11,18))}`, `script: []`. **Nothing was held for 30,000 frames**, and the wedged leg was a **warp leg toward ROUTE18**, not the final leg. (Note: this is exactly the telemetry commit bf6b501 was written to capture, and it is what closed Seat B's central inference — the enrichment paid for itself on the first event that needed it.)
- **R11** — upstream `ensure_facing_direction("Right")` driven ALONE from the stall state, 1,000-frame cap: did **not** return; **0 buttons pressed**; `running_state == NOT_MOVING` on **0/1000** frames; facing still "Down". Seat B's mechanism CONFIRMED as observation.
- **R14** — holding B on the slope: `(running_state, tile_transition)` histogram over 600 frames = **{(0,0): 599, (0,2): 1}**, position unchanged. B **does** restore `NOT_MOVING`, empirically confirming the `!JOY_HELD(B_BUTTON)` branch of `src/bike.c`. (`walking.py:148-149` deliberately sets `run=False` on a bike, i.e. upstream disables the one input that would suppress the pull.)
- **R12 / RA4-PRE — Seat B's own falsifier FIRED.** `uncollected_item_balls((3,35))` returns `[]` and `run_skill(collect_item_balls((3,35)))` completes immediately with no stall. Cause, from the ROM object templates: **ROUTE17 has ten objects and every one is a trainer** (`Route17_EventScript_Virgil/Isaiah/Raul/Nikolas/Billy/Jamal/Zeek/Corey/William/Jaxon`) — **zero `EventScript_Item` objects**. Controls: ROUTE16 has 10 templates including `EventScript_CutTree` (flag 18) and `Route16_EventScript_Snorlax` (flag 128); ROUTE18 has 3, all trainers. Route 17's five FRLG items on Bulbapedia are **hidden** items, which `uncollected_item_balls` (`items_ground.py:15-33`, filtering on `"EventScript_Item" in script_symbol`) correctly does not enumerate. Fix A's precondition is **structurally unmeetable**, not merely unmet in this save.
- **R13 / RA5** — full ROM scan of **425 FRLG maps** for behaviours `"Cycling Road Pull Down"`/`"…Grass"`: exactly one hit, **ROUTE17 (3,35) with 2,107 such tiles**. `_FORCED_SLOPE_MAPS == frozenset({(3,35)})` is **provably exact**.

Exchange 3 — Seat C (Quality Gate, Codex GPT-5-class), verdict: **PASS-WITH-CONCERNS, explicitly conditional** — "the proposal survives only after **dropping Fix A**. If Fix A remains part of the proposed action, my verdict becomes **FAIL**." Independently re-verified the load-bearing citations. **Demonstrated stall**: "ship nothing" is the right call — current code routes Route 17 through `walk_carefully()` via `_FORCED_SLOPE_MAPS` (`navigation.py:821`/`:947`/`:963`) and that path is bounded (`:738`, `:754`, `:791`, `:808`). **Fix A: FAIL/drop**, confirming the driver's R12 refutation from the code (`items_ground.py:15`/`:26`/`:51`/`:73`) and naming the hidden-item-vs-item-ball conflation as the §5 defect. **Fix B**: mechanism accepted (re-verified that `walking.py:172` runs before timeout accounting starts at `:179`, and the unbounded gate at `:467-479`), but **rejects the press-anyway-after-120-frames variant** and rules for the **conservative raise-only** shape — "do not press arbitrary directions after grace across 96 unaudited call sites"; it also **declined a generic hold-B-during-turn** despite R14, citing `walking.py:148` (upstream deliberately disables run/B while biking), while allowing that "a Route-17-specific B-hold helper could be a later design, but it is not the safer general upstream patch." **Fix C: PASS**, with the assertion specified exactly (scan for the two behaviour names, assert the map set equals `navigation._FORCED_SLOPE_MAPS`; tile names at `map.py:388`/`:390`). **Independent confirmations**: Seat B's `patches/` layering claim (`.gitignore:8`, `setup.sh:7`/`:17`, 7 patched files not including `walking.py`); Seat B's `planner.py:144` strict-`>` argmax argument, including that Route 16 (`dependencies.json:134`) precedes Route 17 (`:150`) so Doduo loses the tie by insertion order; and that Seat A's four gates are off the historical path. No forbidden scope fetched.

Exchange 4 — Seat B (closing amendment): **Fix A WITHDRAWN unconditionally and on the record** — "the withdrawal is not a concession to the verdict, it is the falsifier firing as specified"; accepts the §5 defect. Draws a consequence the driver had not: `collect_item_balls` was the **only** dynamic-map caller it could find, so the rest-assuming-interaction class now has **zero demonstrated live paths** → re-ranks RR-1 HIGH → **LOW-latent**, and notes R13 **retires RR-3 outright** (the allowlist is proven exact, not believed exact), leaving RR-2 (96 unaudited call sites) as the top residual — which *strengthens* rather than weakens the "ship nothing for the demonstrated stall" ruling, since the whole action now reduces to a bound plus a test. **Accepts raise-only Fix B as the shipping shape**, and states plainly what it loses: it no longer *repairs* the Cycling Road turn (the ROM-sourced `BikeInputHandler_Slope` TURNING branch is left unexploited) — on Route 17 the leg **fails** rather than turns, trading a silent 30,000-frame freeze for a named bounded failure at ≤300 frames. Supplies an absorption chain with arithmetic (see the §5 refutation below). On **R10's warp leg**: the historical wedge was on the `navigation.py:963` branch, not `:947`; both evaluate `_has_spinners(position[0]) or _has_spinners(<target>)` and `position[0]==(3,35)` short-circuits True at `:829-830` either way, so the guard that actually remediated this event is the one at `:963` and R5's PASS exercises that branch; Fix B's scoping is unaffected since `ensure_facing_direction` sits below both via `walking.py:175`. R10+R11 convert its §1.2 mechanism from inference to observation. **Route-17-specific B-hold recorded as rejected-for-now with a reopen condition** (reopen if a live forced-slope stall is one Fix B converts into repeated bounded failure rather than progress, i.e. the `navigation.py:988-989` cap is hit on Route 17 live). **No dissent.**

Exchange 5 — Seat C (final): **verdict CONFIRMED on the amended proposal**, with one new §5 refutation and a corrected bound. **Claim 4 (`:963` vs `:947`): ACCEPTED** — the direct final branch is `navigation.py:945-951`, the warp-leg branch `:959-968`, both short-circuit Route 17 via `_has_spinners` at `:824-830`, so the `:963` branch is what remediated R5. **Claim 3 (absorption): PARTLY ACCEPTED, with corrected arithmetic and a REFUTATION (§5).** Accepted: `TimedOutTryingToReachWaypointError` is a subclass of `BotModeError` (`walking.py:103`) and upstream catches only that subclass (`:377-382`), so a plain `BotModeError` from `ensure_facing_direction` reaches dexbot's handler at `navigation.py:983` and re-raises only at `interruptions > 30` (`:987-989`) = the 31st attempt; on R1's controllable slope the generic path breaks at `:1020-1021` before yielding. **Corrected numbers: the worst bound is `31×300 + 30×120 = 12,900` frames, not Seat B's `31×420 = 13,020`** (the 31st attempt re-raises *before* the `:1019` wait), and the expected bound under the observed R1 condition is nearer `31×300 = 9,300`; both are well under `_PROGRESS_BUDGET_FRAMES` so the watchdog is not the failure boundary. **REFUTED**: Seat B's "…through `runner.py:491-493` into planner deferral" is **not generally true** — `run_skill` logs and **re-raises the original exception** at `:491-493`, it does not wrap `BotModeError` into `SkillError` (`SkillError` is dexbot-local at `runner.py:16-20`; upstream `BotModeError` is an unrelated bare `Exception` at `_interface.py:214-215`), and `planner.py:604` catches only `SkillError`. Only paths with an intermediate boundary that converts — e.g. `catch_species`'s `except (BotModeError, SkillError)` at `catching.py:620-631`, which re-raises `SkillError` — reach planner deferral; **top-level story/gym navigation does not**. **RR-1 re-ranking: justified only as "no demonstrated live path remains after Fix A was withdrawn" — NOT as "no dynamic-map caller exists"**; semi-dynamic paths into rest-gated behaviour remain (`planner.py:469-474`, `catching.py:577-648`, `planner.py:194-205`), and R12 removes the Route 17 item-ball instance without proving the class absent.

Driver verification of Seat C's exchange-5 refutation: confirmed against the repo. `runner.py:491-493` is `except BaseException as e: _log_event(...); raise` — it re-raises the original exception unwrapped; `BotModeError(Exception)` at `_interface.py:214-215` is unrelated to `SkillError(RuntimeError)` at `runner.py:16-17`; `planner.py:604` reads `except SkillError as e:`. Seat C is right, and the consequence is load-bearing for implementation: **were raise-only Fix B shipped, a `BotModeError` escaping `navigate_to`'s 31-attempt cap would propagate past `run_skill` and past the planner's `except SkillError`** on any objective that lacks an intermediate converter — so an implementer must either convert at the `navigation.py:988-989` re-raise or widen the planner's catch. This refines (does not invalidate) Fix B; it is recorded as concern (i) below.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9's replay/regression gate inapplicable; nothing implemented). The PASS attaches to: (i) the classification — CODE DEFECT, unbounded upstream turn loop (`ensure_facing_direction`) whose at-rest press gate is unsatisfiable on FRLG Cycling Road, entered only because a live NPC forced an off-facing first waypoint, and **already remediated in mainline** by `_FORCED_SLOPE_MAPS` + `walk_carefully` (specifically the warp-leg branch at `navigation.py:963`); (ii) the ruling to make **NO runtime change for the demonstrated stall**, which R5 proves passes today; (iii) **FIX A WITHDRAWN** — its own falsifier fired (R12); (iv) **raise-only FIX B** as separable defence-in-depth against the class, shipped through `patches/` as the 8th file of `patches/0001-upstream-fixes.patch` regenerated in the same commit; and (v) **FIX C**, the ROM-scan assertion, which R13 shows passes today. Were this live: (ii) means the demonstrated objective needs no fix commit and the bot resumes; (iv) and (v) would open as separate, replay-gated changes — §9 replay for Fix B = drive `ensure_facing_direction` on the slope and assert a named `BotModeError` within ≤300 frames plus a healthy-ground control asserting ≤30-frame return and no behaviour change; regression still 74/2 (75/2 with Fix C's new test).
Vote: single terminal verdict from Seat C (Quality Gate), weight 4; Seats A and B did not dissent from the classification or the fix direction (A classified and pinned the tile-model gap; B relocated the wedge to the exact unbounded loop, sourced it to the decompilation, and withdrew its own centrepiece when the driver falsified it). No verdict disagreement to resolve under §7. Dissent: **none** — Seat B's one non-dissenting note is recorded: it accepts losing Fix B's repair property as a deliberate, evidence-backed trade, preserved as concern (ii)'s reopen condition rather than discarded.
Concerns (recorded intact, never deleted):
(i) **Seat C's §5 refutation — the absorption chain is overstated.** A final plain `BotModeError` is NOT converted by `run_skill` (`runner.py:491-493` re-raises unwrapped) and is NOT caught by `planner.py:604` unless an intermediate caller such as `catch_species` (`catching.py:620-631`) converts it to `SkillError`. Driver-confirmed. An implementer of Fix B must close this or the bound leaks past the deferral machinery on story/gym paths. Seat B's frame arithmetic is also corrected: worst case **12,900** frames (`31×300 + 30×120`), expected **≈9,300**, not 13,020 — the third `interruptions`-based arithmetic slip this council has logged.
(ii) **Raise-only Fix B does not repair Cycling Road turning; it only bounds the freeze.** If a live Route 17 path repeatedly hits `navigation.py:988-989`, reopen the Route-17-specific B-hold repair design (R14: holding B restores `NOT_MOVING`, {(0,0):599,(0,2):1} over 600 frames) with targeted tests.
(iii) **The 96 unaudited call sites (37 dexbot, 59 upstream) are the top implementation risk.** Raise-only is acceptable *because* it avoids arbitrary post-grace presses, but changing `ensure_facing_direction` from never-raising to sometimes-raising still needs focused regression coverage — named targets: `tests/test_m3_navigation.py` (all four route through `follow_waypoints:175`), `test_m5_catching.py`/`test_c_catch_action.py` (`catching.py:220`), the eight `test_m7_*` gym tests (`gyms.py:98`/`:498`), `test_m8_secret_key.py` (`story.py:705`).
(iv) **RR-1 is LOW-latent, not retired.** "No demonstrated live caller" is evidence for lower priority, not proof of nonexistence: semi-dynamic paths into rest-gated behaviour remain at `planner.py:469-474`, `catching.py:577-648` and `planner.py:194-205`. R12 removes the Route 17 item-ball instance only.
(v) **Fix C must ship with the exact assertion R13 supports** — scan FRLG maps for the two behaviour names and assert the resulting map set equals `navigation._FORCED_SLOPE_MAPS`. Every guard in this event points at that constant, and R9 shows it otherwise rests on an undatable comment.
(vi) **Seat A's blast-radius claim was over-broad and is corrected on the record**: the `spin()`-on-Route-17 residual it rated MEDIUM/live is not planner-reachable — ROUTE17 is the strict argmax for none of its five land species (Seat B's table, Seat C-verified, R8-corroborated). The `spin()` gate *is* genuinely unsatisfiable on the slope (R7: 0 presses in 6,000+3,000 frames, `stop_condition` never evaluated), so the hazard is real but has no caller.
(vii) **Disclosed reproduction artifact**: the driver's listener-less hand-drive re-wedged at ROUTE17 (8,136) on a pinned `EventScript_DoTrainerBattle`; `TrainerApproachListener` (`_listeners.py:288-307`) B-mashes that script under `run_skill`'s per-frame listener pump (`runner.py:333-334`), which is why R5 completed. Both Seats A and B concur this is an artifact, not a defect. The residual is that the invariant *"any Route 17 skill must be driven through `run_skill`"* exists only in this note.
(viii) **All savestate-derived facts are driver-provided memory-state observations** — the artifacts live outside the checkout and Seat C could not read them. The objective label is scrubbed and the `skills.jsonl` is synthesized; the coordinate coincidence between this event's `(11,18)` and the incident named in `navigation.py:813-820`'s comment is recorded as **context only, never as evidence of label identity** (R9: the guard's introduction is undatable from git).
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written. No commit, no PARKED routing). Were this live: **no fix commit for the demonstrated stall** (current code passes it, replay-confirmed at 12,352 frames); raise-only Fix B and Fix C would each open as separate replay-gated changes. **NOT a PARKED objective** — the Cycling Road route exists in-game, completes under current code, and nothing here is a re-derivable dead end.
Finding: none (dry-run must not write FINDINGS.md; were this a live event, a finding would record: a deterministic silent OVERWORLD freeze while cycling at ROUTE17 (11,18) facing Down, because a stationary NPC on the downhill tile (11,19) forced `calculate_path` to emit an off-facing first waypoint, so `follow_waypoints` (`walking.py:172-175`) called `ensure_facing_direction`, whose at-rest press gate (`walking.py:472-477`) is unsatisfiable on Cycling Road — pret `src/bike.c` `BikeInputHandler_Normal` writes `runningState = MOVING` every frame on `MB_CYCLING_ROAD_PULL_DOWN` (0xD0) without B held, and `MetatileBehavior_IsForcedMovementTile()` excludes 0xD0/0xD1 so `PLAYER_AVATAR_FLAG_FORCED` is never set — leaving an unbounded loop that pressed **zero** buttons for the full 30,000-frame budget (`held_buttons: []` at the dump); remediated in mainline by `_FORCED_SLOPE_MAPS`/`walk_carefully` at `navigation.py:963`, with the residual class bounded by a raise-only budget on `ensure_facing_direction` and the allowlist frozen by a ROM-scan assertion — sources as above. A second finding would record that `map_path.py` models eight forced-movement tile classes but not Cycling Road, so `_has_spinners`'s generic detector structurally cannot see ROUTE17 — with the reopen condition that a *deterministic landing* model for the per-frame, B-suppressible, input-overridable pull is ever shown to exist.)

Driver disclosures (side effects, all restored):
- Ran eleven headless read-only reproductions on the two `stall_h` savestates via the persistent-profile emulator (`dexbot.emulator.setup_headless_emulator`, `DEXBOT_VIDEO=0`, `is_test_run=True`): (1) full RAM decode of both dumps (state/coords/facing/avatar flags/`is_on_bike`/running+transition state/controllability/player map-object flags/script context/tasks/party/bag/key items/live map objects); (2) the tile-behaviour grid, held-direction tests (each direction ×300f), idle sampling, and tap-and-settle tests; (3) `calculate_path` probes to seven destinations; (4-6) the hand-driven `navigate_to` A/B, a yield-from-chain trace of the listener-less re-wedge, and the (8,136) dead-state battery (A/B/Start presses, 5,000-frame idle, 3,000-frame holds); (7) **the decisive A/B through the real `dexbot.runner.run_skill` frame loop** with bot listeners and the watchdog live; (8) `spin()` drives at two Route 17 tiles; (9) `planner.missing_catchable()`/`accessible_maps()` probes; (10) Seat B's R10/R11/R12/R13/R14 probes including a 425-map ROM behaviour scan; (11) ROM object-template enumeration for ROUTE17 plus ROUTE16/ROUTE18 controls. Every write was sandboxed: `runner._events_path` redirected to a scratch dir **outside** the repository, `runner._checkpoint_phase` and `runner._dump_stall` monkeypatched to no-ops (the latter replaced by a capturing stub for R10), and `context.emulator.create_save_state` stubbed — so no `current_state.ss1`, no `fixtures/_stalls` (the directory still does not exist), no `fixtures/_phases` writes, and `logs/` is still empty. The only telemetry produced went to `%TEMP%\dryrun008_scratch\skills.jsonl` (9 lines). The in-memory `_FORCED_SLOPE_MAPS` emptying used for the PRE-GUARD arm was restored in-process and never touched a file. (Cf. memory `stall-ss1-omits-controller-state`: this OVERWORLD **nav** freeze **IS** reconstructible from the `.ss1` — like DRYRUN-007 and unlike DRYRUN-001/002/003 — because the blocker is a live map object in game RAM and the wedged loop is a pure function of that RAM.)
- `pokebot-gen3/profiles/livingdex/` **already existed** as found (`metadata.yml` md5 8809e386fbe047116a9ad0dfe102e931, `stats.db` md5 e446606a5d81ea6dc06481a5e3b23e61); both are byte-identical after all runs and **no `current_state.ss1` was written**. `pokebot-gen3/profiles/` is exactly as found (10 loose config files + `__pycache__` + `livingdex`). (Cf. memory `repro-writes-profile-dir`.)
- Both evidence savestates are byte-intact (md5 fd8e0c8c9bb26737dc998636cbe51f65 and 703c72ec1095329830a31038d0581f84, identical before and after every run).
- Repro scripts were written to a scratch directory **outside** the repository (`%TEMP%\dryrun008_scratch`), never into the checkout.
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, `data/`, `patches/`, or any `dexbot/` or `pokebot-gen3/` source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md; `git -C pokebot-gen3 status` shows exactly the 7 files the pre-existing `patches/0001-upstream-fixes.patch` modifies, unchanged from as-found.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`, and the invocation confirms there is no bot process to pause or resume, so no close-write is needed).

---

## EVENT DRYRUN-009 — 2026-08-01T17:33:32Z
Trigger: §4.1 watchdog_stall — the runner progress watchdog (`dexbot/runner.py:39` `_PROGRESS_BUDGET_FRAMES`=30_000, OVERWORLD budget selected at :415-425; sampled every 2,000 frames at :407; dump+raise at :458-463) dumped a savestate for objective 'stall_i' (dry-run: historical, scrubbed name) with the progress sample `((3, 49), (12, 13), 19972, 7, 0, 7, (217, 67, 184, 0, 119, 0))` = map (3,49)=`MapFRLG.THREE_ISLAND_PORT` ("THREE ISLE PORT", Route, 48×20) / coords (12,13) / money ₽19,972 / 7 Poké Balls / 0 Great Balls / game_state 7 OVERWORLD / party-HP. Evidence: `stall_i.ss1` (md5 19a522619697b0bee5f08f272dbd50a8), `stall_i_state.json`, and a harness-**synthesized** `skills.jsonl` carrying exactly one `stall` (`"script": []`), one `error` and one `deferred` record, all reading `made no observable progress for 30000 frames at ((3, 49), (12, 13))`; **no pre-stall telemetry exists**, and per the invocation its absence is treated as unknown, not as absence of events. `logs/skills.jsonl`, `logs/progress.jsonl`, `STATUS.md` absent (pre-run harness). The dump predates commit bf6b501, so `controller_stack`/`held_buttons`/`nav_intent` are absent from the artifact; the driver reconstructed them by replay (R7). State: avatar on foot, **facing Up**, `AvatarFlags.OnFoot|Controllable`, running/tile-transition both NOT_MOVING, controllable, **no script active**, only idle tasks; party Blastoise L100 217/286 (Strength/Rock Smash/Surf/Dig), Jynx L23, Dragonite L59 (Cut/Waterfall), Hypno L50 **0**/159, Arbok L44, Marowak L33 **0**/83; badges 1-8; Rainbow Pass, HM01-04/06/07, all three rods; **dex owned 136 / seen 175**; `SYS_GAME_CLEAR` and `SYS_NATIONAL_DEX` both True. **This stall is fully reconstructible from the .ss1 and was reproduced byte-exactly** (R7).

Seats: A=claude-sonnet-5  B=claude-opus-5[1m]  C=Codex GPT-5-class
Model check A: required §3 = claude-sonnet-5. The spawn interface accepts only aliases, not the full §3 IDs, so spawned via alias `sonnet`; self-reported `claude-sonnet-5`. MATCH.
Model check B: required §3 = claude-opus-5 (Seat B default). Spawned via alias `opus`; self-reported `claude-opus-5[1m]` (the 1M-context harness variant). MATCH — the harness limitation recorded as a VIOLATION in DRYRUN-001…006 no longer applies. Seat B declined Fable escalation (§3 ruling in exchange 2).
Model check C: required §3 = Codex GPT-5-class, a different provider than Seat B. Codex over MCP (account-default model; the explicit `gpt-5.x` override is not forced — prior dry-runs showed the ChatGPT-account backend rejects it). Self-reported "Codex, GPT-5-based coding agent". Cross-model gate (§3): Codex ≠ Claude → SATISFIED. One Codex thread for the whole event, opened at event open per §5 and continued with the continuation tool for every subsequent turn: threadId **019fbe1d-0ad3-79e2-849e-74cf5cfc3d66**.

Exchanges: 5/6

Exchange 1 — Seat A (Diagnostician), position: **CODE DEFECT, in two layers, plus a latent third.** NOT a KB error as the primary cause, NOT a transient (R7 reproduces deterministically), NOT already-known (FINDINGS/DECISIONS/PARKED all empty). **PRIMARY/ROOT — in the vendored pokebot-gen3**: the pathfinding graph reads collision/elevation from a static ROM-only map table that can never see a runtime layout swap — `map.py:1756-1796` populates `_map_header_cache` **once ever** (`if len(...) == 0:` at :1766) from the ROM `gMapGroups` table (:1775); `map.py:745-755` caches the layout struct again in a process-global `_map_layout_cache`; `map_path.py:582-616`/`:85-94` build the whole cross-map graph from `get_map_data` once per ROM into a module-global `_maps`, never invalidated; only `get_map_data_for_current_position()` (`:1742-1750`) reads the live `gMapHeader`, and pathfinding never calls it for arbitrary tiles. Seat A independently re-fetched pret's `ThreeIsland_DunsparceTunnel/scripts.inc` rather than take the driver's word, and was explicit that this layer is **code-reading-supported, not artifact-supported**. It noted the consequence also wears "planner deadlock" clothing: `planner._graph_reachable` (`:43-66`) returns False for (3,49) and `missing_catchable`'s gate (`:134-138`) silently drops the map. **SECONDARY/PROXIMATE — dexbot's own code**: `catching.py:599` computes `feasible`; `:600-603` fast-fails **only on the rod path**; `:604` `candidates = feasible or candidates` lets the land/spin path fall back to the unfiltered doomed list — the exact churn the comment at `:587-590` says the filter exists to prevent. **TERTIARY/LATENT (explicitly not this artifact)**: `runner.py:225-227`'s SIGALRM step-watchdog is a no-op on Windows, so a genuine zero-yield hang has no safety net. **MINOR KB note**: `dependencies.json` "3,49" requires only `BADGE08_GET` and never mentions National Dex. Severity: one full 30,000-frame cycle plus ~26k `_walkable` calls, deterministic. Blast radius: demonstrated = this map/species; latent = any FRLG map with a runtime `setmaplayoutindex`, plus the `catching.py` asymmetry for *any* all-infeasible spin candidate set. Seat A supplied five numbered open questions and three challenges to the driver's own pack (below), and re-verified every cited line reference against the repo.

Driver probes requested by Seat A, executed read-only and sandboxed between exchanges 1 and 2:
- **OQ3 (live-vs-static inside the tunnel) — first attempt returned a FALSE NEGATIVE, disclosed.** R15 read the static reader first, which re-poisoned `modules.map._map_layout_cache`; R16 re-ran with the live read first and got the decisive result below. The driver's first probe was wrong and is recorded as such.
- **OQ5 — answered YES.** From the east pocket (38,5), `catch_species("Dunsparce",(3,49))` immediately navigated to the grass at (32,7) and encountered a Dunsparce (dex `seen` flipped True). The catch pipeline is healthy once past the reachability step.
- **OQ1 — no position helps.** `_plan_via_graph` to the grass returns None from ten plausible positions (pier, port north, tunnel west mouth, Three Island town, Three Island PC, harbor arrival (8,2) and pier (8,5), Bond Bridge, Vermilion, One Island) and a route ONLY from inside the east pocket.
- **R17a — Seat A's R4-vs-R7 contradiction challenge resolved: not racy, destination-specific.** The zero-yield hang reproduced twice (100 s and 70 s wall clock, frame counter never leaving 0).

Key driver measurements underlying the whole event (R1-R18): (12,13)'s tile type is **"South Arrow Warp"** and it is the port's warp to `THREE_ISLAND_HARBOR`, i.e. the **arrival tile** of the harbor warp — which is why facing stayed Up and position never changed; hold-Down fires the arrow warp into the harbor and hold-Up walks north off-map, so the avatar was **not trapped**; all 17 land encounter tiles are grass at **(31-36, 6-8)** at the far EAST end, and `data/encounters.json` for 3,49 is a **Dunsparce-only** land table (`land_encounter_rate` 1) with Dunsparce **unowned and unseen**; `_graph_reachable((3,49))` is **False while the bot stands on (3,49)**; `_walkable` succeeds pier→(16,4) and (38,5)→(32,7) but fails **inside** THREE ISLE PATH (3,4)→(25,5). **R7** reproduced the artifact byte-exactly through the real `run_skill` loop (identical sample tuple and error text; `held_buttons: []`, `controller_stack: ['DexSkillMode.run']`, `nav_intent: None`, `script: []`). **R8** counted, over that window: `navigate_to` 5×, `_plan_warp_route` 398×, `_walk_out_of_pocket` 132×, `_plan_via_graph` 403×, `_walkable` 26,174×, with the main-thread stack always inside `run_single_frame()`. **R10/R12/R13** hand-drove the bot through the tunnel — (3,4)→(3,3)→east to (20,3)→(20,1)→(25,1)→down through the (25,5) warp→emerged at PORT (38,5), 12 waypoints from the grass — proving **the route is physically walkable in this save** while `get_map_data` calls every crossed tile collision=1. **R16** then measured why: the static ROM header layout pointer for (2,34) is `0x083451F8`, the live `gMapHeader` pointer is `0x083453C0`, and through the live header those tiles read `coll=0 elev=3` with (25,5) reading **"South Arrow Warp"** instead of **"Cave"** — which is why `navigation.py:77`'s `WARP_BEHAVIOURS` filter drops the `TUNNEL(25,5)→PORT(38,5)` edge, leaving the Dunsparce grass with no incoming edge from anywhere in the game.

Exchange 2 — Seat B (Fix Designer), position: **agrees with the classification and corrects Seat A on three counts, one substantive.** (1) *Substantive*: "`map.py:757-772` derives collision from it" is line-drifted and **mislocates the failure** — collision/elevation are derived at `:780-781`, but the thing that destroys the warp edge is **tile behaviour**, computed at `:795-809` by dereferencing the layout's *tileset* pointers (`_map_layout[16:20]`/`[20:24]`) and surfaced via `_tile_behaviour` (`:813-815`); a fix correcting only collision would still leave `navigation.py:77`'s filter dropping the edge. (2) *Strengthening*: `_get_all_maps_metadata()` calls `get_map_data` at `map_path.py:601` and reads `map_size` at `:615`, and `map_size` (`map.py:850-851`) reads `_map_layout[:4]` — so **the graph build populates the layout cache from static ROM headers for every map in the game**, which makes R15's poisoning the *normal* runtime ordering rather than a probe artifact. (3) Seat A's frame arithmetic was double-labelled; the clean derivation is that `interruptions` is per-call (`navigation.py:909`) and `_walk_out_of_pocket` runs once per cycle (`:939`), so R8's **132 = 132 interruption cycles total** ≈ 26.4 per call × ~227 frames ≈ 30,000 — and `catch_species` would have raised its own clean `SkillError` (`catching.py:631`) ~4,000 frames later. **New artifact-level proof Seat A missed**: inside this skill the only `_plan_via_graph` callers are `catching.py:599` and `_plan_warp_route`, so **403 − 398 = 5** = the five candidates, with `navigate_to` entered exactly 5× — `catching.py:604` executing with `feasible == []` is *measured*, not inferred. **Disagreement with Seat A (§5)**: "recurrence is self-limiting" is wrong — an empty queue drives `planner.py:527-536` to `return caught`, **silently ending the run**, which is worse than the logged stall. **Third defect Seat B adds**: the two consumers of graph availability disagree in opposite directions — `planner.py:52-53` returns **True** when the graph is unavailable while `navigation.py:416-418` returns **None** — so a missing graph admits every annotated map *and* makes every candidate infeasible. **Proposal, ranked: FIX 1** (ship first, defence in depth) — symmetric fast-fail at `catching.py:600-604`, **gated on `_load_nav_graph() is not None`**, because an unconditional fast-fail would turn a missing graph from a degraded mode into a total catching outage; **FIX 2** (load-bearing) — re-key `_map_layout_cache` by `(rom, map, layout_pointer)`, register a `_layout_override` when `get_map_data_for_current_position()` sees the live pointer diverge, and invalidate `PathMap._tiles` / `_warp_edges` / `_walkable_cache` / the cached epoch section; **FIX 3** (load-bearing) — a *derived* variant-layout table so unvisited maps can be planned, with the boundary that **tile bytes are always read from the ROM at the variant layout's own address and nothing about collision/elevation/behaviour is transcribed by hand**; **FIX 4** (load-bearing) — add `SYS_NATIONAL_DEX` (+ the two Seafoam flags) to `story_epoch()` (`:269-289`), without which the cached `nav_graph.json` section still encodes the un-dug tunnel. Layering verified by Seat B itself: `pokebot-gen3/` is gitignored, `setup.sh` pins a commit and applies `patches/*.patch` **with a trailing `|| true` that silently swallows a malformed patch**, so any upstream edit must be regenerated into `patches/0001-upstream-fixes.patch` in the same commit. **Seven alternatives rejected in writing** (do nothing; KB-only; PARK; hardcoded `_FLAG_DOORS`-style override; live-header-only; dexbot-side monkey-patch; blanket optimism), each with a mechanism-level reason. **Fable escalation (§3): NO** — Seat A classified cleanly, no Seat C FAIL at that point, and all changes are patches to existing subsystems. Seven residual risks ranked; six driver probes requested.

Driver probes requested by Seat B, executed read-only and sandboxed between exchanges 2 and 3:
- **P1 — FALSIFIED Seat B's leading OQ1 hypothesis.** In a fresh process with no prior pathfinding call: `HIDE_ROUTE_12_SNORLAX` True, **`HIDE_ROUTE_16_SNORLAX` False**, `HIDE_SAFFRON_ROCKETS` True, `SYS_NATIONAL_DEX` True; `story_epoch()` = **`'8+snx12+silph'`**, that section **IS** on disk, and `_load_nav_graph()` returns a real 1,082-entry dict. Seat B's "no `+snx16` section → graph unavailable → optimistic True" precondition does not hold. P1 also measured **`STOPPED_SEAFOAM_B4F_CURRENT` True and `STOPPED_SEAFOAM_B3F_CURRENT` False** in this save.
- **P6 — but the mechanism is sufficient.** With `_load_nav_graph` forced to None, `missing_catchable()` returns a queue of exactly **one** entry: `Dunsparce (3,49) rate 100 spin`.
- **P2 — validates the enumeration method.** Of 365 `*_Layout` symbols in the shipped pret symbol table, 309 are referenced by a static map header; the 56 unreferenced are RS/prototype/duplicate leftovers **plus exactly** `ThreeIsland_DunsparceTunnel_DugOut_Layout`, `SeafoamIslands_B3F_CurrentStopped_Layout`, `SeafoamIslands_B4F_CurrentStopped_Layout`, `SevenIsland_House_Room1_DoorOpen_Layout` and 16 `TrainerTower_*`.
- **P3 — the class generalises.** Seafoam B3F read through the variant layout differs from the static read on 8 sampled tiles, e.g. (24,9)/(21,12)/(24,12) `'Southward Current'`→`'Ocean Water'`, (27,12) `'Fast Water'`→`'Ocean Water'`.
- **P4 — Seat B's residual risk 5 retired.** Dunsparce static and DUG_OUT layouts are both 30×7; Seafoam B3F/B4F static and variant are all 38×24, so no variant shifts the global coordinate frame.
- **P5 not run separately** — Fix 1's benefit is bounded by R8: the guard fires after the five `_plan_via_graph` calls at `catching.py:599` (~0.1 s each, 0 frames) and before the first `navigate_to`.
- **Driver-verified for Seat B's severity claim**: `planner.py:525-536` — an empty queue with no deferrals executes `return caught`. With the graph present `missing_catchable()` is `[]` in this save and Dunsparce is the only KB-catchable species left, so **current code would end the run believing the dex is finished**.

Exchange 3 — Seat C (Quality Gate, Codex), verdict: **FAIL.** Axis verdicts: classification PASS-WITH-CONCERNS (main defect class sound, independently verified at `catching.py:587`, `navigation.py:63`, `planner.py:43`, `map.py:745`; concern = OQ1 unresolved); **citation quality FAIL**; coherence PASS-WITH-CONCERNS; alternatives PASS ("specific and mostly supported by code paths, especially `_FLAG_DOORS` being passability-only in `map_path.py:914`"). The §5 defect, quoted: Fix 3's Seafoam/tunnel condition mappings are "load-bearing game facts … the proposal does not give the pret script URL/path or another allowed §5 source for those conditions. R16/P2/P3 support that variant layouts exist and matter; they do not prove the condition mapping. A promise that the generated table will have 'each with a pret citation' is not itself a citation in the written proposal." Two smaller corrections: `.gitignore:8` not `:7`; `map_path.py:914` not `:915`. Seat C nonetheless **upheld** Fix 1's `graph_available` guard ("right as a scoped compatibility choice"), **upheld** Fix 3's read-bytes-from-ROM boundary ("I would fail any implementation that transcribes collision, elevation, or tile behavior into JSON"), **narrowed** Seat B's "guaranteed 30,000-frame burn per objective" as too broad, and **narrowed** the ordering claim ("mostly correct but overstated — not literally inert for live map reads or future graph generation"). It ruled OQ1 not large enough to invalidate the diagnosis but large enough to keep classification at concerns, and named the single closing item: the pre-objective admission snapshot.

**DRIVER ERROR, disclosed on the record.** Part of that FAIL was caused by the driver, not by Seat B: when briefing Seat C at exchange 3 the driver summarised Seat B's proposal but **omitted its Sources block entirely**. Seat B's exchange-2 answer *did* carry the pret raw URLs for the Dunsparce tunnel (`IsNationalPokedexEnabled` → `setmaplayoutindex LAYOUT_..._DUG_OUT`) and for Seafoam **B3F** (`call_if_set FLAG_STOPPED_SEAFOAM_B3F_CURRENT` → `setmaplayoutindex LAYOUT_..._CURRENT_STOPPED`), both fetched and verified by Seat B. So two of the three conditions Seat C failed as uncited had in fact been cited, and Seat C judged them on an incomplete record. The driver disclosed this to Seat C verbatim at exchange 5 and told it not to defer to the driver's framing. **The defect was not wholly an artifact of the driver's error**: Seat B cited B3F and did **not** cite B4F, which was in its shipped table — Seat B agrees that is an independent §5 defect. Both facts are recorded; neither cancels the other.

Exchange 4 — Seat B (defect closure), position: **accepts the FAIL and closes it without re-opening anything Seat C upheld.** Accepts both line corrections, and volunteers that Seat C's `map_path.py:914` *block* citation is the better one because the passability-only semantics are enforced at `:919`. **Owns the B4F defect without hiding behind the driver's error**: "`SeafoamIslands_B4F` was in my shipped table with `FLAG_STOPPED_SEAFOAM_B4F_CURRENT` asserted from a *symbol name* … and an inference from the B3F script, not from the B4F script. A symbol name proves a setter exists; it does not prove which flag guards it." Now quotes the decisive lines of all three shipped conditions from pret, including `SeafoamIslands_B4F_OnTransition::`'s `call_if_set FLAG_STOPPED_SEAFOAM_B4F_CURRENT, ..._SetNoCurrentLayout` → `setmaplayoutindex LAYOUT_SEAFOAM_ISLANDS_B4F_CURRENT_STOPPED`, and states the rule **no condition ships without a quoted script** (Seven Island and Trainer Tower are therefore in the enumeration but explicitly NOT in the shipped table). **Self-withdrawal it volunteered**: the B4F script shows Articuno is gated by a *separate* flag `FLAG_FOUGHT_ARTICUNO`, so it withdraws its own exchange-1 phrasing "the current-stopped swap is the precondition for reaching Articuno" as a causal claim the script does not support, and marks as unmeasured inference its line about the graph believing in vanished currents. **Accepts both of Seat C's narrowings on the record**, restating: "The invariant is the loss of the filter, not the frame count", and "Fix 4 is load-bearing for this stall's code path, not for the general validity of Fixes 2 and 3." **Restructures Fix 3 so the condition is not hand-written at all**: derive the table from ROM bytecode (resolve `ScrCmd_setmaplayoutindex`'s opcode index in `gScriptCmdTable`, walk each map's `MAP_SCRIPT_ON_TRANSITION`, collect reachable `setmaplayoutindex <u16>` with its guarding `call_if`/`goto_if` operand, resolve through `gMapLayouts`) — flag-guarded entries are then fully derived and offline-evaluable via `get_event_flag_by_number` (`modules/memory.py:346`, which notably lacks the silent-False-on-unknown-name behaviour of `get_event_flag` at `:337-338`), leaving one ROM-sourced special-index→predicate mapping, with **safe degradation** for unrecognised specials: mark the map *layout-volatile*, suppress cached negative reachability and force the live-observation hook, so an unknown swap costs one bad plan corrected on arrival rather than silent false confidence. Its §5 argument for the residue: "Fix 3 is a prediction, Fix 2 is the measurement that audits it", and it invites Seat C to fail any entry not auditable that way. **OQ1: hypothesis withdrawn on the record** — P1 killed every leg; new ranking is (1) *process-memo* graph unavailability during the admission window (`_nav_graphs[epoch]`/`_rebuild_attempted` memoise for the process lifetime at `navigation.py:344-353`), offered as **unfalsifiable by any savestate probe and therefore unsupported rather than supported**; (2) position skew (`planner.py:526` vs `:596`), ranked lower because with the section present the east pocket has no incoming warp edge from anywhere; (3) unknown. It adopts Seat C's conclusion that **the artifact cannot settle OQ1**, and per §8.1 converts the dead end into an action: **a fifth shipment item, telemetry only** — `_log_event` at the admission boundary (`planner.py:547-549`) and at `catch_species` entry recording `story_epoch()`, graph availability, queue length, chosen objective, position and `len(feasible)`. Keeps as the durable artifact-level finding: 403 − 398 = 5 with `navigate_to` ×5 is the signature of `catching.py:592`'s five-candidate spin loop, so admission ran through `missing_catchable()` → `choose_objective`.

Driver probes requested by Seat B, executed between exchanges 4 and 5:
- **P7 — the derivation is implementable.** `gScriptCmdTable` 0x0815F9B4..0x0815FD08 (213 slots), `gSpecials` 0x0815FD60..0x08160450 (444 slots). **`ScrCmd_setmaplayoutindex` (0x0806AA48) → opcode index 167**; **`IsNationalPokedexEnabled` (0x0806E25C) → special index 403**; `call_if`=7, `goto_if`=6, `specialvar`=38, `end`=2, `return`=3.
- **P8 — HEURISTIC, noise disclosed by the driver rather than dressed up.** A byte-scan of every map's ON_TRANSITION script for opcode 167 **without** a per-command operand-length table. It independently hits, from the ROM's own bytecode, `THREE_ISLAND_DUNSPARCE_TUNNEL → 0x083453C0`, `SEAFOAM_ISLANDS_B3F → 0x0833B6EC`, `SEAFOAM_ISLANDS_B4F → 0x0833BE30`, `SEVEN_ISLAND_HOUSE_ROOM1 → 0x08338B4C` — corroborating Seat B's three cited conditions — and also emits obvious garbage (four Tanoby Ruins chambers and `POKEMON_LEAGUE_LORELEIS_ROOM` "resolving" to layouts they cannot own, `SEAFOAM_ISLANDS_1F` inheriting B3F's, `UNDERGROUND_PATH_NORTH_SOUTH_TUNNEL → layout id 520 → 0x00000000`). **Corroboration for the four named maps only; NO evidence either way on completeness**, which is the actual OQ2 question.
- **P3′ — NOT RUN; Seat B's offered falsifier of its own root cause stands UNEXECUTED.** It requires standing on Seafoam B3F/B4F *in this savestate*; from the stall tile that is a cross-region trek (sail to Vermilion, then Surf Routes 19-20), disproportionate for a dry run, and no fixture savestate is in Seafoam (nearest `m7_badge_blaine.ss1` at CINNABAR_ISLAND_GYM), while any other save carries different flag state and would not test the asked question. Recorded as a gap, not as a pass.

Exchange 5 — Seat C (final), verdict: **PASS-WITH-CONCERNS** on the amended proposal. **Citation quality: PASS on the amended record** — "the prior FAIL is cured by the restored Sources block plus the new B4F citation", and Seat C **independently fetched and checked the pret B3F and B4F scripts itself**, confirming each sets its stopped-current layout under its own `FLAG_STOPPED_SEAFOAM_B{3,4}F_CURRENT` and that B4F separately gates Articuno on `FLAG_FOUGHT_ARTICUNO`. Classification: PASS-WITH-CONCERNS ("the tunnel root cause is directly measured by R16/R10/R12/R13; the Seafoam generalization is plausible and now sourced, but not live-measured on this artifact"). Coherence: **PASS** ("Fix 1 bounds the local stall, Fixes 2/3 repair map layout resolution, Fix 4 makes the precomputed graph reflect those state dimensions, and the new telemetry item directly addresses the unresolved admission path"). Alternatives: **PASS** ("Seat B accepted the overbroad claims and narrowed them correctly"). It **accepts the restructured Fix 3 under §5** and accepts the "prediction audited by measurement" argument, "because the shipped table is no longer a hand-written game-fact table of collision/behavior/layout bytes … Fix 2's live `gMapHeader` comparison is an in-run falsifier" — while holding that it "would still fail any entry whose predicate is hand-written without a script/ROM source, or any JSON that transcribes collision/elevation/tile behavior instead of resolving ROM layout bytes." It rules the unexecuted P3′ and P8's noise **sufficient to keep classification at concerns rather than a clean PASS**, and specifies the shipping bar for the Seafoam entries: "either a proper bytecode walker with command operand lengths, or a targeted live-header/checkpoint validation on Seafoam B3F/B4F showing the active layout pointer matches the derived condition in a save where the relevant flags are set." It rules Seat B's telemetry item the right §8.1 action and that the missing admission snapshot **should not block the fixes**, "because none of the four behavioral fixes depends on which admission hypothesis is true." **No remaining §5 refutation survives on the amended record.** No forbidden scope fetched.

Verdict: **PASS-WITH-CONCERNS** (on the written proposal, per dry-run rules — §9's replay/regression gate inapplicable; nothing implemented). The PASS attaches to: (i) the classification — a two-layer CODE DEFECT whose root is that pokebot-gen3 models FRLG map geometry as a pure function of the ROM while FRLG swaps whole layouts at runtime (`setmaplayoutindex`), so THREE ISLE PATH reads as solid rock and its east warp reads "Cave" instead of "South Arrow Warp", leaving the Dunsparce grass with **no incoming warp edge from anywhere in the game**; and whose proximate layer is `catching.py:604` restoring graph-infeasible candidates on the land/spin path only; (ii) **FIX 1** (graph-gated symmetric fast-fail) as the first, smallest, ship-first change; (iii) **FIX 2** (observation-aware layout cache + invalidation) and the **restructured FIX 3** (ROM-derived variant-layout table, one ROM-sourced special mapping, layout-volatile degradation) as the load-bearing repair; (iv) **FIX 4** (`story_epoch()` gains `SYS_NATIONAL_DEX` + the two Seafoam flags), load-bearing *for this stall's code path*; and (v) the **admission-boundary telemetry** as the §8.1 action for an OQ1 the artifact cannot settle. Were this live: the demonstrated objective would ship Fix 1 plus the telemetry first and resume; Fixes 2+3+4 would ship together behind §9 replay (assert `get_map_data((2,34),(20,3)).collision == 0`, `(25,5).tile_type == "South Arrow Warp"`, the `TUNNEL(25,5)→PORT(38,5)` edge present, and the bot reaching a grass tile in (31-36,6-8)) plus regression at 74/2 and a regenerated `patches/0001-upstream-fixes.patch` and `data/nav_graph.json` in the same commit. **NOT a PARKED objective** — Dunsparce is genuinely catchable in this save (the driver physically walked the whole route) and it is the only remaining KB-catchable species.
Vote: single terminal verdict from Seat C (Quality Gate), weight 4, after one FAIL→returned→cured cycle. Seats A and B did not dissent from the final classification or fix direction; Seat B's §5 disagreement with Seat A ("recurrence is self-limiting") was resolved in Seat B's favour by driver verification of `planner.py:525-536`. No verdict disagreement to resolve under §7. Dissent: **none**.
Concerns (recorded intact, never deleted):
(i) **The Seafoam runtime-layout class is sourced and plausible but NOT live-measured on this artifact.** Before those entries ship as production graph data Seat C requires either a proper bytecode walker with command operand lengths, or a live-header/checkpoint validation on Seafoam B3F/B4F in a save where the flags are set. Seat B's own offered falsifier (P3′) went **unexecuted** because Seafoam is unreachable from this savestate without a cross-region trek and no fixture is there — recorded as a gap, not a pass.
(ii) **OQ1 is unresolved and cannot be closed from this artifact.** The stall dump proves the selected `catch_species` path stalled; it does not prove the admission state that let Dunsparce into the queue. P1 falsified Seat B's leading hypothesis; its surviving hypothesis (process-lifetime memoisation of a failed graph load at `navigation.py:344-353`) is unfalsifiable by any savestate probe and is recorded as unsupported, not supported. The admission-boundary telemetry is required so the next occurrence diagnoses itself.
(iii) **Fix 1 deliberately preserves graph-absent optimism.** Acceptable as compatibility — an unconditional fast-fail would turn a missing graph into a total catching outage — but graph-unavailable admission remains a degraded mode and must be visible in telemetry.
(iv) **Driver transmission error (disclosed above).** The driver's exchange-3 briefing omitted Seat B's Sources block, which caused two of the three conditions in Seat C's FAIL to be judged as uncited when they had been cited. Seat C re-judged on the complete record at exchange 5. The B4F citation gap was genuine and independent of the error.
(v) **A driver probe returned a false negative before it returned the truth.** R15 reported "no live-vs-static mismatch" only because it read the static reader first and re-poisoned `_map_layout_cache`; R16 reversed the order and produced the decisive measurement. Seat B's exchange-2 audit then showed the poisoning is the *normal* runtime ordering (`map_path.py:601`/`:615` touches every map's layout on the first pathfinding call), so the false negative was itself evidence.
(vi) **A separate, deterministic, more severe latent defect was found and is NOT part of this proposal**: `navigate_to(THREE_ISLAND_HARBOR, (8,3))` from the stall tile is a **zero-yield infinite loop** — the emulator frame counter never leaves 0 (R4, reproduced at R17a), because upstream's `waypoint_generator` (`walking.py:347-375`) exits immediately when already at the destination, `follow_waypoints` yields zero times, and dexbot's loop re-plans the identical single-leg route forever. No frame-based watchdog can fire, and `runner.py:225-227`'s SIGALRM guard does not exist on Windows. This needs its own event; Seat B sketched a portable `threading.Timer` + `interrupt_main()` replacement.
(vii) **The KB entry for "3,49" is incomplete but inert**: it requires only `BADGE08_GET` while the tunnel's passability is National-Dex-gated. Both seats agree it neither causes nor fixes this failure. Related hazard recorded: `get_event_flag` silently returns False for names absent from the table (`modules/memory.py:337-338`), so a typo in a `requires` list makes a map permanently invisible with no error.
(viii) **All savestate-derived facts are driver-provided memory-state observations.** The artifacts live outside the checkout and Seat C could not read them; the objective label is scrubbed and the `skills.jsonl` is synthesized. Seat C independently verified the repo citations and fetched the pret scripts itself.
Fix: none (diagnose-only dry-run — IMPLEMENT NOTHING; the proposal remains written. No commit, no PARKED routing).
Finding: none (dry-run must not write FINDINGS.md; were this a live event, two findings would record: (1) a deterministic 30,000-frame OVERWORLD stall at THREE_ISLAND_PORT (12,13) — the arrival tile of the harbor warp, which is why facing stayed Up and position never changed — because `catch_species("Dunsparce",(3,49))` fed five graph-infeasible grass candidates into `navigate_to` anyway (`catching.py:604`, land path only), each burning ~26 plan-retry cycles × ~227 frames while pressing **zero** buttons, and would have raised its own clean `SkillError` ~4,000 frames after the watchdog preempted it; (2) the root cause — pokebot-gen3 resolves map geometry from the static ROM map header (`map.py:745-755`, `:1756-1796`) and caches it per (rom, map), so FRLG's runtime `setmaplayoutindex` is invisible: for THREE ISLE PATH the live `gMapHeader` layout pointer is `0x083453C0` (`ThreeIsland_DunsparceTunnel_DugOut_Layout`) while the static one is `0x083451F8`, making a corridor the bot physically walked read as collision=1 and its east warp read "Cave", so `navigation.py:77` drops the `TUNNEL(25,5)→PORT(38,5)` edge and the Dunsparce grass becomes unreachable in the graph — with the extension condition that the same defect is already live on `SEAFOAM_ISLANDS_B4F` in this save (`STOPPED_SEAFOAM_B4F_CURRENT` True) — sources: pret `data/maps/ThreeIsland_DunsparceTunnel/scripts.inc`, `data/maps/SeafoamIslands_B3F/scripts.inc`, `data/maps/SeafoamIslands_B4F/scripts.inc`, Bulbapedia "Three Isle Path", and the repo/ROM reads above).

Driver disclosures (side effects):
- Ran twenty read-only sandboxed reproductions (R1-R20) on `stall_i.ss1` via the persistent-profile emulator (`dexbot.emulator.setup_headless_emulator`, `DEXBOT_VIDEO=0`, `is_test_run=True`): full RAM decode; port/tunnel/harbor geometry, warp tables and live map objects; held-direction probes; `calculate_path`/`_walkable`/`_plan_via_graph`/`_plan_warp_route` probes; the byte-exact stall reproduction through the real `run_skill` frame loop with listeners and watchdog live; instrumented call counters and main-thread stack sampling; two runs of the zero-yield hang; a physical hand-driven traverse of THREE ISLE PATH proving the route; the live-vs-static layout measurement; a ROM layout-table scan; and Seat B's P1-P8. Writes were sandboxed: `runner._events_path` redirected to a scratch dir **outside** the repository, `runner._checkpoint_phase` no-op'd, `runner._dump_stall` replaced by a capturing stub, and `context.emulator.create_save_state` stubbed — so no `current_state.ss1`, no `fixtures/_stalls` (the directory still does not exist), no `fixtures/_phases` writes, and `logs/` is still empty. Repro scripts were written to `%TEMP%\dryrun009_scratch`, never into the checkout.
- **SIDE EFFECT NOT FULLY RESTORED, disclosed.** The sandbox stubbed savestate and telemetry writes but **not `context.stats`**, so when the R14 reproduction reached the Dunsparce grass and triggered a real wild encounter, pokebot's `StatsDatabase` recorded it into `pokebot-gen3/profiles/livingdex/stats.db`: one `encounter_summaries` row (Dunsparce, 1 encounter, 0 catches) and one `shiny_phases` row, both stamped 2026-08-01T16:55:05Z. The driver deleted exactly those two rows and vacuumed; **all tables are now empty as found** (`encounter_summaries` 0, `shiny_phases` 0, `encounters` 0, `pickup_items` 0, `base_data` 0), but SQLite byte-identity is not recoverable after a delete+VACUUM: the file md5 went `e446606a5d81ea6dc06481a5e3b23e61` (as found) → `71c7f845eda98e890b709308808a49f6` (after the runs) → `30d0de39f49313bffb4f35a1689ce5e1` (after restoration). `metadata.yml` is byte-identical (8809e386fbe047116a9ad0dfe102e931) and no `current_state.ss1` was written. **A future dry-run sandbox should stub `context.stats` as well.** (Cf. memory `repro-writes-profile-dir`.)
- One reproduction (R20's first attempt) segfaulted libmgba by reading an out-of-ROM address; it was re-run with bounded reads. No state was affected — the crashed process held only the in-memory emulator.
- The evidence savestate is byte-intact (md5 19a522619697b0bee5f08f272dbd50a8, identical before and after every run).
- No writes to STATE.md, COUNCIL_LOG.md, FINDINGS.md, DECISIONS.md, PARKED.md, config.json, `data/`, `patches/`, or any `dexbot/` or `pokebot-gen3/` source. `git status` shows only this untracked COUNCIL_LOG.dryrun.md; `git -C pokebot-gen3 status` shows exactly the 7 files the pre-existing `patches/0001-upstream-fixes.patch` modifies, unchanged from as-found.
- STATE.md left unchanged (dry-run do-not-write list; it already reads `Open event: none`, and the invocation confirms there is no bot process to pause or resume, so no close-write is needed).
